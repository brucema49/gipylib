# INS 初始化指导方案

> 实现 GNSS/INS 组合导航的初始化流程：当匹配到 GNSS 数据时，先将 IMU 数据插值到 GNSS 观测时间戳，再根据配置执行不同的初始化模式（静态初始化、动态初始化-速度矢量、动态初始化-位移矢量、配置设定），完成 GNSS 定位数据对 INS 的初始化后，方可进入机械编排与组合导航。
> 参考 `tools/KF-GINS` 的 `imuInterpolate` / `initialize` / `isToUpdate` / `newImuProcess` 实现。
> 参考 `tools/gnss_ins_lc_nhc` 的 `navinitialized.cc` 中 `StartAligning` / `AcceLeveling` / `MotionAligned` / `InterpolateGnssVel` / `SetInitialVariance` 实现。
> 参考 `tools/GINav/src/ins/ins_init.m` 的初始状态与协方差装配。
>
> **时间系统约定**：全框架内部统一使用 **Unix 时间戳（float 秒，与 rtklib-py `gtime_t.time + gtime_t.sec` 一致）**。
> 所有时间戳（`imu.timestamp`、`gnss.timestamp`、`t0`/`t1`/`t2`/`t_gnss`）均为 Unix 秒，时间差 `dt` 通过相减直接得到。
> 时间转换工具：`src/core/time_utils.py`（`gpst_to_unix` / `unix_to_gpst`，`GPST_EPOCH_UNIX = 315964800`）。
>
> **坐标系约定**（与 [imu.md](file:///home/mxl/workplace/gipylib/skills/imu.md) 一致）：
> - 机械编排与初始化主计算在 **E 系（ECEF）** 下进行
> - 体坐标系 b 系：FRD（前-右-下）
> - 车体坐标系 v 系：FRD，与 b 系通过安装角旋转矩阵 `R_b^v` 关联（仅 pitch/yaw 两维，roll 假设为 0）
> - 导航系 n 系：ENU（仅输出与中间量转换用）
> - 姿态角顺序：航向(yaw) → 俯仰(pitch) → 横滚(roll)
>
> **当前实现状态**：
> - ✅ 已实现：`src/stream/imu_sensor.py::ImuSensor`（IMU 流式读取，含 RFU→FRD 坐标系转换）、`src/stream/internal_gnss_sensor.py::InternalGnssSensor`（RTK 实时解算）、`src/log/aligner.py::Aligner`（IMU 积攒 + GNSS 收割匹配器，已实现路径 C 数据对齐管线）
> - ✅ 已实现：`src/core/ins/initializer.py::InsInitializer`（三种初始化模式：静态 / 速度矢量 / 位置差分）
> - ✅ 已实现：`src/core/ins/interpolator.py`（`is_to_update` / `imu_interpolate` / `find_bracket_imus`）
> - ✅ 已实现：`src/core/ins/earth_param.py`（`ecef2llh` / `cal_Ce2n` / `gravity_ecef` + WGS84 常量）
> - ✅ 已实现：`src/core/ins/attitude.py`（`euler2dcm` / `dcm2quat` / `att_caln2e`）
> - ✅ 已测试：`tests/ins/test_static_init.py` / `test_velocity_init.py` / `test_position_diff_init.py`（三种模式均通过新阈值检验）
> - 🚧 预留：`InsCore` 机械编排核心类、`LcIntegration` 双滤波 EKF（详见 [机械编排.md](file:///home/mxl/workplace/gipylib/skills/机械编排.md) 与 [estimator.md](file:///home/mxl/workplace/gipylib/skills/estimator.md)）
>
> **本文档范围**：仅描述初始化阶段的指导方案，**不编写代码**。机械编排详见 [imu.md](file:///home/mxl/workplace/gipylib/skills/imu.md)，EKF 融合详见 [estimator.md](file:///home/mxl/workplace/gipylib/skills/estimator.md)。

---

## 目录

- [1. 概述](#1-概述)
- [2. 模块文件与参考映射](#2-模块文件与参考映射)
- [3. 初始化触发条件](#3-初始化触发条件)
- [4. IMU 数据插值到 GNSS 时间戳](#4-imu-数据插值到-gnss-时间戳)
- [5. 初始化模式分类](#5-初始化模式分类)
  - [5.4 动态初始化默认模式选择（基于 GNSS 解算模式）](#54-动态初始化默认模式选择基于-gnss-解算模式)
- [6. 配置设定初始化（SETTING）](#6-配置设定初始化setting)
- [7. 静态初始化](#7-静态初始化)
- [8. 动态初始化 - 速度矢量初始化](#8-动态初始化---速度矢量初始化)
- [9. 动态初始化 - 位移矢量初始化](#9-动态初始化---位移矢量初始化)
- [10. 静止启动到动对准（STATIONARY_AND_MOTION）](#10-静止启动到动对准stationary_and_motion)
- [11. 初始状态装配](#11-初始状态装配)
- [12. 初始协方差矩阵设置](#12-初始协方差矩阵设置)
- [13. 初始化完成与机械编排衔接](#13-初始化完成与机械编排衔接)
  - [13.6 Reboot 识别与重新初始化](#136-reboot-识别与重新初始化)
- [14. 决策流程总图](#14-决策流程总图)

---

## 1. 概述

### 1.1 设计目标

INS 初始化是组合导航的入口阶段，目标是在系统启动后，**当首次匹配到 GNSS 数据时**，利用 GNSS 解算结果（位置/速度）和 IMU 观测数据，确定 INS 的初始位置、初始速度、初始姿态以及 IMU 误差参数，并装配初始协方差矩阵，为后续 EKF 机械编排与组合导航提供可信的初始状态。

**核心要求**（用户原话）：
1. 当匹配到了 GNSS 数据，需要将 IMU 数据插值到 GNSS 观测数据的时间戳
2. 插值方法参考 `tools/KF-GINS` 的 `imuInterpolate`（增量切分法）
3. **插值时 GNSS 时间戳之前要有 IMU 数据，后面也要有一个 IMU 数据，除非刚好一致**（详见第 4.5 节）
4. 插值完成后，根据设置执行不同的初始化：
   - 静态初始化
   - 动态初始化 - 速度矢量初始化
   - 动态初始化 - 位移矢量初始化
   - 配置设定初始化（SETTING）
5. **动态初始化默认模式选择**（详见第 5.4 节）：
   - SPP 模式 → 默认使用速度矢量初始化（通过多普勒测速得到的速度）
   - RTK/RTD 模式 → 默认使用位置差分初始化
   - 动态初始化需要设置运动阈值
   - 动态模式下 GNSS 数据不提供速度值时，统一使用位置差分法
6. 完成 GNSS 定位数据初始化 INS 后，才开始准备机械编排、组合导航事宜
7. **初始化模式需要识别 reboot**：第一次初始化后，长时间没有 GNSS 观测时还要继续进行初始化，与数据对齐统一调度识别 reboot（详见第 13 节）
8. **位置差分初始化的 GNSS 历元缓冲**：采用位置差分初始化方法时，需维护一个长度为 `gnss_buffer_size`（默认 3）的 GNSS 历元滑动窗口；当运动阈值达到时，确保缓冲区已存满 3 个历元，但**只用最新的两个历元位置解**计算位置差分速度（详见第 9.3 节）

### 1.2 设计原则

| 原则 | 说明 |
|------|------|
| **GNSS 主导** | 初始位置/速度优先使用 GNSS 解算结果，姿态由对准过程或配置确定 |
| **IMU 辅助对准** | 静态初始化用加速度计感知重力方向确定水平角；MEMS IMU 不能感知地球自转，方位角需借助 GNSS 速度或配置 |
| **配置优先级最高** | `use_set_posvelatt` 或 `use_set_attitude` 启用时，直接使用配置值，跳过自动对准 |
| **插值先于对准** | 必须先将 IMU 数据插值到 GNSS 时间戳，再做对准计算，保证时间一致性 |
| **插值前后包夹** | GNSS 时间戳前后必须各有一个 IMU 历元（包夹区间），除非 GNSS 时间戳与某 IMU 历元刚好一致；不满足时延迟初始化，等待更多 IMU 数据 |
| **GNSS 模式决定动对准方式** | SPP 默认速度矢量初始化（多普勒测速），RTK/RTD 默认位置差分初始化；GNSS 不提供速度时统一用位置差分 |
| **位置差分需历元缓冲** | 位置差分初始化需维护 `gnss_buffer_size`（默认 3）长度的 GNSS 历元滑动窗口；运动阈值达到时确保缓冲区存满 3 个历元，但只用最新两个历元计算差分速度 |
| **坐标系统一** | 全部在 E 系（ECEF）下进行，对准过程中产生的 n 系中间量立即转回 E 系 |
| **双滤波协方差独立** | 主滤波 P1 与 NHC 子滤波 P2 各自装配初始协方差，互不影响 |
| **Reboot 可重入** | 初始化可重复触发：长时间 GNSS 中断后识别 reboot，重新进入初始化流程；与 `Aligner` 的 reboot 调度统一识别 |
| **失败可降级** | 自动对准失败时，可降级为配置设定模式或继续等待数据 |

### 1.3 与现有模块的关系

```
AlignedBlock (来自 Aligner)               ← 路径 C 数据对齐管线输出
   ├── gnss: GnssSolution (timestamp, pos, vel, cov, mode)
   └── imu_list: List[ImuMeasurement]
                    ↓
            InsInitializer.initialize()
                    ↓
   ┌────────────────────────────────────┐
   │ 1. IMU 插值到 GNSS 时间戳           │
   │ 2. 模式选择 (SETTING/静态/动态)     │
   │ 3. 对准计算 (姿态/位置/速度)        │
   │ 4. 装配初始状态                     │
   │ 5. 装配初始协方差 P1 / P2           │
   └────────────────────────────────────┘
                    ↓
            InsState (初始状态)
                    ↓
       进入 LcIntegration 机械编排循环
```

初始化模块位于数据对齐管线（`Aligner`）与 EKF 融合估计器（`LcIntegration`）之间：
- **上游**：`Aligner.harvest()` 输出的 `AlignedBlock`（含 GNSS 解 + 一个 GNSS 历元窗口内的 IMU 列表）
- **下游**：`LcIntegration.process_epoch()` 接收初始化完成的 `InsState`，进入机械编排 + EKF 主循环

---

## 2. 模块文件与参考映射

### 2.1 计划模块文件

| 文件 | 职责 | 参考 | 实现状态 |
|------|------|------|---------|
| `src/core/ins/initializer.py` | `InsInitializer` 类：插值、模式选择、对准、状态装配，三阈值检验（静态速度 / 动态速度 / 动态角速度） | KF-GINS `initialize` + gnss_ins_lc_nhc `StartAligning` | ✅ 已实现 |
| `src/core/ins/interpolator.py` | `imu_interpolate` / `is_to_update` / `find_bracket_imus`（与 [imu.md 5.4-5.5](file:///home/mxl/workplace/gipylib/skills/imu.md#54-imu-增量切分参考-kf-gins-imuinterpolate) 共用） | KF-GINS `imuInterpolate` / `isToUpdate` | ✅ 已实现 |
| `src/core/ins/earth_param.py` | `ecef2llh` / `cal_Ce2n` / `gravity_ecef` / WGS84 常量 | gnss_ins_lc_nhc `navearth.hpp` | ✅ 已实现 |
| `src/core/ins/attitude.py` | `euler2dcm` / `dcm2quat` / `att_caln2e` | gnss_ins_lc_nhc `navattitude.hpp` | ✅ 已实现 |

### 2.2 参考代码映射

| 参考项目 | 参考函数/方法 | 本项目对应概念 |
|---------|-------------|---------------|
| KF-GINS `gi_engine.h::imuInterpolate` | 增量切分静态方法 | `imu_interpolate`（详见第 4 节） |
| KF-GINS `gi_engine.h::isToUpdate` | 4 种时间对齐情况判断 | `is_to_update`（详见第 4 节） |
| KF-GINS `gi_engine.cpp::initialize` | 状态与协方差装配 | `InsInitializer.initialize`（详见第 11、12 节） |
| KF-GINS `kf_gins.cpp::loadConfig` | 从 YAML 读取初始 PVA/IMU 误差/噪声 | 配置加载（详见第 11 节） |
| gnss_ins_lc_nhc `navinitialized.cc::StartAligning` | 对准主入口，模式分发 | `InsInitializer.align`（详见第 5-10 节） |
| gnss_ins_lc_nhc `navinitialized.cc::AcceLeveling` | 加速度计水平对准 | `acce_leveling`（详见第 7 节） |
| gnss_ins_lc_nhc `navinitialized.cc::MotionAligned` | GPS 速度计算航向 | `motion_aligned`（详见第 8 节） |
| gnss_ins_lc_nhc `navinitialized.cc::InterpolateGnssVel` | GNSS 位置差分计算速度 | `interpolate_gnss_vel`（详见第 9 节） |
| gnss_ins_lc_nhc `navinitialized.cc::SetInitialVariance` | 初始协方差设置 | `set_initial_variance`（详见第 12 节） |
| gnss_ins_lc_nhc `navinitialized.cc::AttCaln2e` | n 系姿态转 E 系 | `att_caln2e`（详见第 11 节） |
| GINav `ins_init.m` | 初始状态/协方差/Q/Φ 装配 | `InsInitializer.initialize` |

---

## 3. 初始化触发条件

### 3.1 触发条件

`LcIntegration` 启动后处于 `UNINITIALIZED` 状态，从 `estimate_queue` 消费 `AlignedBlock`。当满足以下条件时触发初始化：

1. **首次匹配到 GNSS 数据**：`AlignedBlock.gnss` 不为 `None`，且 `GnssSolution.mode` 为有效解（`SPP`/`RTD`/`RTK_FIXED`/`RTK_FLOAT`/`External`）
2. **IMU 数据已积攒**：`AlignedBlock.imu_list` 至少包含 1 个 `ImuMeasurement`
3. **配置就绪**：初始化相关配置（`alignnment_posvelatt_mode`、`alignnment_attitude_mode`、`static_speed_threshold`、`dynamic_speed_threshold`、`angular_velocity_threshold_deg` 等）已加载

### 3.2 数据来源

| 数据 | 字段 | 来源 |
|------|------|------|
| GNSS 位置 | `gnss.pos` (ECEF, m) | `GnssSolution.pos`（来自 `InternalGnssSensor` 内部解算或外部文件） |
| GNSS 速度 | `gnss.vel` (ECEF, m/s) | `GnssSolution.vel`（同上；若 GNSS 仅有位置，则由位移差分计算，见第 9 节） |
| GNSS 协方差 | `gnss.cov` | `GnssSolution.cov`（用于初始位置/速度协方差） |
| GNSS 时间戳 | `gnss.timestamp` (Unix 秒) | `GnssSolution.timestamp` |
| IMU 列表 | `imu_list` (List[ImuMeasurement]) | `AlignedBlock.imu_list`（一个 GNSS 收割窗口内的 IMU 数据） |
| IMU 时间戳 | `imu.timestamp` (Unix 秒) | `ImuMeasurement.timestamp` |
| IMU 角速度 | `imu.gyro` (rad/s, b 系) | `ImuMeasurement.gyro` |
| IMU 加速度 | `imu.accel` (m/s², b 系) | `ImuMeasurement.accel` |

### 3.3 触发后的处理顺序

```
InsInitializer.initialize(aligned_block, config) -> InsState:

  1. 提取 GNSS 数据 + IMU 列表
  2. 【第 4 节】IMU 数据插值到 GNSS 时间戳
     - 若需要，对 IMU 列表做增量切分，得到 t_gnss 时刻的 IMU 历元
  3. 【第 5 节】根据配置选择初始化模式
  4. 【第 6-10 节】执行对应模式的对准计算
     - SETTING: 直接读取配置
     - 静态: AcceLeveling + GNSS 位置
     - 动态-速度矢量: MotionAligned (GNSS 速度)
     - 动态-位移矢量: InterpolateGnssVel + MotionAligned
     - STATIONARY_AND_MOTION: 监测速度阈值，超限后切换
  5. 【第 11 节】装配初始状态 (位置/速度/姿态/IMU误差)
  6. 【第 12 节】装配初始协方差 P1 / P2
  7. 返回 InsState，进入机械编排主循环
```

---

## 4. IMU 数据插值到 GNSS 时间戳

> **核心要求**（用户原话）：初始化时当匹配到了 GNSS 数据，需要将 IMU 数据插值到 GNSS 观测数据的时间戳，具体插值方法参考 `tools/KF-GINS`。
>
> 本节与 [imu.md 第 5 节](file:///home/mxl/workplace/gipylib/skills/imu.md#5-imugnss-插值与时间对齐) 和 [estimator.md 第 9 节](file:///home/mxl/workplace/gipylib/skills/estimator.md#9-时间同步与-imu-插值) 描述的插值方法**完全一致**，但**初始化阶段的插值目的不同**：
> - 机械编排阶段：插值是为了在 GNSS 历元时刻执行量测更新
> - 初始化阶段：插值是为了让 IMU 数据与 GNSS 数据时间对齐，便于对准计算（如 AcceLeveling 用插值后的 IMU 平均值）

### 4.1 问题背景

GNSS 解算历元（1 Hz）与 IMU 采样历元（100~200 Hz）通常不对齐。当 GNSS 量测时刻 `t_gnss` 落在两个 IMU 时刻 `t0` 和 `t2` 之间时，需要将 IMU 数据"切分"到 `t_gnss` 时刻，保证对准计算时使用时间一致的 IMU/GNSS 数据。

### 4.2 KF-GINS 的增量切分法

参考 [tools/KF-GINS/src/kf-gins/gi_engine.h](file:///home/mxl/workplace/gipylib/tools/KF-GINS/src/kf-gins/gi_engine.h) 的 `imuInterpolate` 静态方法：

```cpp
static void imuInterpolate(const IMU &imu1, IMU &imu2, const double timestamp, IMU &midimu) {
    if (imu1.time > timestamp || imu2.time < timestamp) {
        return;  // 时间戳不在区间内，不插值
    }
    double lamda = (timestamp - imu1.time) / (imu2.time - imu1.time);

    midimu.time   = timestamp;
    midimu.dtheta = imu2.dtheta * lamda;   // 前半段角增量
    midimu.dvel   = imu2.dvel * lamda;     // 前半段速度增量
    midimu.dt     = timestamp - imu1.time;

    imu2.dtheta = imu2.dtheta - midimu.dtheta;  // imu2 原地保留后半段
    imu2.dvel   = imu2.dvel - midimu.dvel;
    imu2.dt     = imu2.dt - midimu.dt;
}
```

**关键设计**：
- **增量切分而非弹出**：将 `imu2` 的增量按比例切分，前半段存入 `midimu`，后半段保留在 `imu2` 中
- **避免数据丢失**：下一次机械编排时，`imu2` 仍保留剩余增量，可与 `imu3` 正常衔接
- **lamda 比例系数**：`lamda = (t_gnss - t0) / (t2 - t0)`，按时间线性分配增量

### 4.3 本项目的适配（速率式 IMU）

本项目 `ImuMeasurement` 使用速率式（`gyro`/`accel`），非增量式（`dtheta`/`dvel`）。**插值时角速度/加速度本身不变，只修改 `timestamp`**，机械编排时通过相邻 timestamp 计算 `dt`：

```
原 KF-GINS（增量式）:                本项目（速率式）:
  dtheta_total = gyro * dt_total       gyro/accel 不变
  dtheta_pre  = dtheta_total * lamda   dt_pre  = t_gnss - t0
  dtheta_post = dtheta_total - pre     dt_post = t2 - t_gnss
                                        dtheta_pre  = gyro * dt_pre
                                        dtheta_post = gyro * dt_post
```

**插值伪代码**（指导方案，非最终代码）：

```
function imu_interpolate(imu_pre, imu_cur, t_gnss) -> midimu:
    # 检查时间戳区间
    if imu_pre.timestamp > t_gnss or imu_cur.timestamp < t_gnss:
        return None  # 不在区间内

    # 创建中间时刻 IMU（前半段，gyro/accel 不变）
    midimu = ImuMeasurement(
        timestamp = t_gnss,
        week      = imu_cur.week,    # week 不变
        accel     = imu_cur.accel,   # 加速度不变
        gyro      = imu_cur.gyro,    # 角速度不变
    )

    # imu_cur 原地保留剩余时段（修改 timestamp 为切分点）
    # 机械编排时通过 imu_cur.timestamp - t_gnss 计算后半段 dt
    imu_cur.timestamp = t_gnss  # 注意：需保留原 timestamp 用于其他计算

    return midimu
```

### 4.4 4 种时间对齐情况（参考 KF-GINS isToUpdate）

参考 [tools/KF-GINS/src/kf-gins/gi_engine.h](file:///home/mxl/workplace/gipylib/tools/KF-GINS/src/kf-gins/gi_engine.h) 的 `isToUpdate` 方法，判断 GNSS 时间戳与 IMU 时间区间的关系：

```
function is_to_update(t0, t2, t_gnss, threshold=1e-3) -> int:
    # t0: imu_pre.timestamp, t2: imu_cur.timestamp, t_gnss: GNSS 时间戳
    if abs(t0 - t_gnss) < threshold:
        return 1  # GNSS 靠近 t0
    elif abs(t2 - t_gnss) <= threshold:
        return 2  # GNSS 靠近 t2
    elif t0 < t_gnss < t2:
        return 3  # GNSS 在中间，需增量切分
    else:
        return 0  # GNSS 时间不在 [t0, t2] 之间
```

**4 种情况的处理**（与 [estimator.md 第 9.7 节](file:///home/mxl/workplace/gipylib/skills/estimator.md#97-四种情况处理流程图) 一致）：

| 情况 | 条件 | 初始化阶段处理 |
|------|------|--------------|
| 0 | `t_gnss` 不在 `[t0, t2]` 之间 | 等待更多 IMU 数据，或外推到 `t_gnss` |
| 1 | `\|t0 - t_gnss\| < 1ms` | 直接使用 `imu_pre` 作为 `t_gnss` 时刻的 IMU 数据 |
| 2 | `\|t2 - t_gnss\| < 1ms` | 直接使用 `imu_cur` 作为 `t_gnss` 时刻的 IMU 数据 |
| 3 | `t0 < t_gnss < t2` | 增量切分：生成 `midimu` 作为 `t_gnss` 时刻的 IMU 数据 |

### 4.5 初始化阶段的特殊处理

与机械编排阶段不同，初始化阶段需要**收集一段时间的 IMU 数据**用于对准（如 AcceLeveling 需要 10 秒静态 IMU 数据做平均）。因此初始化阶段的插值策略是：

1. **首次 GNSS 到达**：从 `imu_list` 中找到包夹 `t_gnss` 的两个 IMU 历元 `imu_pre` / `imu_cur`
2. **执行增量切分**（情况 3）或直接取邻近历元（情况 1/2），得到 `t_gnss` 时刻的 IMU 数据
3. **若需要更多 IMU 数据**（如 STATIONARY_AND_MOTION 模式需要持续监测）：继续消费 `estimate_queue`，维护一个长度为 `data_rate * 10` 的 IMU 缓冲区（参考 `navinitialized.cc:199-202`）
4. **缓冲区满或速度超阈值**：执行对准计算

### 4.5.1 插值前后包夹规则（强制要求）

> **用户原话**：插值时 GNSS 时间戳之前要有 IMU 数据，后面也要有一个 IMU 数据，除非刚好一致。

**强制约束**：初始化阶段执行 IMU 插值时，必须满足以下条件之一，否则**延迟初始化**，继续等待更多 IMU 数据：

| 条件 | 说明 | 处理方式 |
|------|------|---------|
| **包夹条件**（情况 3） | `imu_pre.timestamp < t_gnss < imu_cur.timestamp` | 执行增量切分，生成 `midimu` 作为 `t_gnss` 时刻 IMU |
| **前对齐条件**（情况 1） | `\|imu_pre.timestamp - t_gnss\| < 1ms` | 直接使用 `imu_pre`，无需插值 |
| **后对齐条件**（情况 2） | `\|imu_cur.timestamp - t_gnss\| < 1ms` | 直接使用 `imu_cur`，无需插值 |
| **不满足**（情况 0） | `t_gnss` 不在 `[imu_pre.timestamp, imu_cur.timestamp]` 区间内，或仅一侧有 IMU 数据 | **延迟初始化**，等待更多 IMU 历元 |

**含义**：
- **GNSS 时间戳之前必须有 IMU 数据**：`imu_pre.timestamp ≤ t_gnss`，且 `imu_pre` 不为 `None`
- **GNSS 时间戳之后必须有 IMU 数据**：`imu_cur.timestamp ≥ t_gnss`，且 `imu_cur` 不为 `None`
- **除非刚好一致**：若 GNSS 时间戳与某 IMU 历元时间戳差值 < 1ms（`time_align_threshold`），视为"刚好一致"，可直接使用该 IMU 历元，无需插值

**为什么必须前后包夹**：
1. **增量切分的前提**：KF-GINS `imuInterpolate` 要求 `imu1.time ≤ timestamp ≤ imu2.time`，否则返回不执行（参考 [gi_engine.h:92-94](file:///home/mxl/workplace/gipylib/tools/KF-GINS/src/kf-gins/gi_engine.h)）。仅有一侧 IMU 数据时无法切分
2. **机械编排连续性**：初始化完成后立即进入机械编排，第一个 `dt` 由 `imucur.timestamp - imupre.timestamp` 计算；若 `imupre` 或 `imucur` 缺失，无法启动机械编排
3. **AcceLeveling 时间平均**：静态对准需要 `t_gnss` 前后一段时间的 IMU 数据做平均，仅一侧数据不足以反映静态状态

**实现策略**：
```
function prepare_imu_for_init(aligned_block, t_gnss) -> (imu_pre, imu_cur, midimu) or None:

  imu_list = aligned_block.imu_list

  # 1. 寻找包夹区间
  imu_pre = None
  imu_cur = None
  for i in range(len(imu_list) - 1):
    if imu_list[i].timestamp <= t_gnss <= imu_list[i+1].timestamp:
      imu_pre = imu_list[i]
      imu_cur = imu_list[i+1]
      break

  # 2. 检查是否找到包夹区间
  if imu_pre is None or imu_cur is None:
    # 不满足包夹条件，延迟初始化
    return None

  # 3. 判断对齐情况
  threshold = 1e-3  # 1ms
  if abs(imu_pre.timestamp - t_gnss) < threshold:
    # 情况 1：刚好与 imu_pre 一致，无需插值
    return (imu_pre, imu_cur, imu_pre)
  elif abs(imu_cur.timestamp - t_gnss) < threshold:
    # 情况 2：刚好与 imu_cur 一致，无需插值
    return (imu_pre, imu_cur, imu_cur)
  else:
    # 情况 3：在中间，执行增量切分
    midimu = imu_interpolate(imu_pre, imu_cur, t_gnss)
    return (imu_pre, imu_cur, midimu)

# 调用方
result = prepare_imu_for_init(aligned_block, gnss.timestamp)
if result is None:
  # 延迟初始化，继续等待下一个 AlignedBlock
  return None  # 不进入初始化流程
else:
  imu_pre, imu_cur, midimu = result
  # 进入对准计算...
```

### 4.6 注意事项

- 插值应在**原始 IMU 测量域**进行（比力、角速度），而非在导航结果域
- 增量切分后采样间隔会变化，严格上不满足 INSMech 的等间隔假设，但影响较小（参考 KF-GINS 注释）
- 边界处理：当 GNSS 历元超出 IMU 缓冲区范围时，使用最近邻外推
- 初始化阶段插值完成后，`imu_cur.timestamp` 被修改为 `t_gnss`，后续机械编排需要重新维护 `imupre`/`imucur` 关系

---

## 5. 初始化模式分类

### 5.1 AlignedMode 枚举

参考 [tools/gnss_ins_lc_nhc/include/imu/navinitialized.h](file:///home/mxl/workplace/gipylib/tools/gnss_ins_lc_nhc/include/imu/navinitialized.h) 的 `AlignedMode` 枚举：

```cpp
enum AlignedMode {
    UNKOWN              = -1,  // 未知（未初始化）
    SETTING             = 0,   // 配置设定模式
    STATIONARY_AND_MOTION = 1, // 静止启动到动对准
    IN_MOTION           = 2,   // 起始即运动，直接动对准
};
```

本项目对应概念（伪代码，仅指导）：

```python
class AlignedMode(IntEnum):
    UNKOWN = -1              # 未知（未初始化）
    SETTING = 0              # 配置设定模式（use_set_posvelatt 或 use_set_attitude）
    STATIONARY_AND_MOTION = 1  # 静止启动，速度超阈值后切换到动对准
    IN_MOTION = 2            # 起始速度超阈值，直接动对准
```

### 5.2 模式选择决策树

参考 [tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc](file:///home/mxl/workplace/gipylib/tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc) 的 `StartAligning` 函数（行 67-230）：

```
StartAligning(aligned_block, config) -> InsState:

  ┌────────────────────────────────────────────────────────────┐
  │ 步骤 0: 加载配置项（IMU 误差、安装角、杆臂等）             │
  │   - initial_gyro_bias / initial_acce_bias                  │
  │   - initial_gyro_scale / initial_acce_scale                │
  │   - initial_imu_angle / initial_imu_leverarm               │
  │   - leverarm (GNSS 天线杆臂)                                │
  └────────────────────────────────────────────────────────────┘
                          ↓
  ┌────────────────────────────────────────────────────────────┐
  │ 步骤 1: 检查 use_set_posvelatt (配置完整 PVA)              │
  │   是 → AlignedMode = SETTING                               │
  │        位置/速度来自配置，姿态来自配置                     │
  │        跳过 GNSS 数据消费，直接装配状态                    │
  │        【第 6 节】                                          │
  └────────────────────────────────────────────────────────────┘
                          ↓
  ┌────────────────────────────────────────────────────────────┐
  │ 步骤 2: 消费数据流，找到第一个 GNSS 历元                   │
  │   - 若 GNSS 仅有位置 (GNSSPOS)，再读一个 GNSS 历元         │
  │     调用 InterpolateGnssVel 计算速度                       │
  │     【第 9 节】                                             │
  └────────────────────────────────────────────────────────────┘
                          ↓
  ┌────────────────────────────────────────────────────────────┐
  │ 步骤 3: 检查 use_set_attitude (配置仅姿态)                 │
  │   是 → AlignedMode = SETTING                               │
  │        位置/速度来自 GNSS，姿态来自配置                    │
  │        【第 6 节】                                          │
  └────────────────────────────────────────────────────────────┘
                          ↓
  ┌────────────────────────────────────────────────────────────┐
  │ 步骤 4: 检查 gnss_data.vel.norm() vs velocity_threshold    │
  │                                                          │
  │   ┌──────────────────────────────────────┐                │
  │   │ > 阈值 → AlignedMode = IN_MOTION    │                │
  │   │         MotionAligned(gnss_data)    │                │
  │   │         【第 8 节】                  │                │
  │   └──────────────────────────────────────┘                │
  │                                                          │
  │   ┌──────────────────────────────────────┐                │
  │   │ ≤ 阈值 → AlignedMode =               │                │
  │   │         STATIONARY_AND_MOTION        │                │
  │   │         持续监测，速度超阈值后对准   │                │
  │   │         【第 10 节】                 │                │
  │   └──────────────────────────────────────┘                │
  └────────────────────────────────────────────────────────────┘
```

### 5.3 配置项对照表

| 配置项 | 类型 | 默认值 | 含义 | 参考来源 |
|--------|------|--------|------|---------|
| `alignnment_posvelatt_mode` | int | 0 | 1=使用配置完整 PVA，0=不使用 | `navinitialized.cc:75` |
| `alignnment_attitude_mode` | int | 0 | 1=使用配置姿态，0=不使用 | `navinitialized.cc:74` |
| `alignnment_velocity_threshold` | double | 0.5 | **已废弃**（保留兼容）：原静止/运动判定阈值 (m/s)，由 `static_speed_threshold` / `dynamic_speed_threshold` 替代（见下表） | `navinitialized.cc:73` |
| `initial_pos` | array[3] | — | 初始位置（纬度/经度/高程，度/度/米） | KF-GINS `initpos` |
| `initial_vel` | array[3] | — | 初始速度（北/东/下，m/s） | KF-GINS `initvel` |
| `initial_att` | array[3] | — | 初始姿态（横滚/俯仰/航向，度） | KF-GINS `initatt` |
| `initial_gyro_bias` | array[3] | [0,0,0] | 初始陀螺零偏（°/hr → rad/s） | `navinitialized.cc:81-82` |
| `initial_acce_bias` | array[3] | [0,0,0] | 初始加计零偏（mGal → m/s²） | `navinitialized.cc:84-88` |
| `initial_gyro_scale` | array[3] | [0,0,0] | 初始陀螺比例因子（ppm） | `navinitialized.cc:90-94` |
| `initial_acce_scale` | array[3] | [0,0,0] | 初始加计比例因子（ppm） | `navinitialized.cc:96-100` |
| `initial_imu_angle` | array[2] | [0,0] | IMU 安装角（pitch/yaw，度） | `navinitialized.cc:102-105` |
| `initial_imu_leverarm` | array[3] | [0,0,0] | IMU 杆臂（b 系，m） | `navinitialized.cc:111-115` |
| `leverarm` | array[3] | — | GNSS 天线杆臂（b 系，m） | `navinitialized.cc:78-79` |
| `use_define_variance_pos_vel` | int | 0 | 1=使用配置位置/速度协方差 | `navinitialized.cc:249` |
| `use_define_variance_att` | int | 0 | 1=使用配置姿态协方差 | `navinitialized.cc:261` |
| `initial_pos_std` | array[3] | — | 初始位置标准差（m） | `navinitialized.cc:252` |
| `initial_vel_std` | array[3] | — | 初始速度标准差（m/s） | `navinitialized.cc:253` |
| `initial_att_std` | array[3] | — | 初始姿态标准差（度 → rad） | `navinitialized.cc:264` |
| `gyro_bias_std` | array[3] | — | 陀螺零偏差（°/hr → rad/s） | `navinitialized.cc:277` |
| `acce_bias_std` | array[3] | — | 加计零偏差（mGal → m/s²） | `navinitialized.cc:278` |
| `gyro_scale_std` | array[3] | — | 陀螺比例因子差（ppm） | `navinitialized.cc:287` |
| `acce_scale_std` | array[3] | — | 加计比例因子差（ppm） | `navinitialized.cc:288` |
| `imu_angle_std` | array[2] | — | 安装角差（度 → rad） | `navinitialized.cc:299` |
| `imu_leverarm_std` | array[3] | — | IMU 杆臂差（m） | `navinitialized.cc:300` |
| `evaluate_imu_scale` | int | 0 | 1=估计 IMU 比例因子 | `navinitialized.cc:283` |
| `evaluate_imu_angle` | int | 0 | 1=估计 IMU 安装角与杆臂 | `navinitialized.cc:294` |
| `data_rate` | int | — | IMU 数据采样率（Hz） | `navinitialized.cc:192` |
| `motion_threshold` | double | 0.5 | **动对准运动阈值** (m/s)，**已废弃**（保留兼容），由 `static_speed_threshold` / `dynamic_speed_threshold` 替代 | 本项目新增（已废弃） |
| `static_speed_threshold` | double | 0.5 | **静态检测速度阈值** (m/s)：GNSS 速度范数 < 此值判定为静态，进入静对准 | 本项目新增 |
| `dynamic_speed_threshold` | double | 4.0 | **动态速度阈值** (m/s)：GNSS 速度范数 > 此值才可进入动对准 | 本项目新增 |
| `angular_velocity_threshold_deg` | double | 30.0 | **动态角速度阈值** (deg/s)：陀螺范数 < 此值才可进入动对准（≈0.5236 rad/s） | 本项目新增 |
| `imu_coordinate_system` | str | "FRD" | IMU 原始坐标系：`"FRD"`=前右下（默认） / `"RFU"`=右前上（读取时转换为 FRD） | 本项目新增 |
| `gnss_velocity_fallback` | str | "doppler" | GNSS 不提供速度时的回退策略：`"doppler"`=多普勒测速，`"position_diff"`=位置差分，`"auto"`=按 GNSS 模式自动选择 | 本项目新增 |
| `gnss_buffer_size` | int | 3 | **位置差分初始化的 GNSS 历元缓冲区大小**：运动阈值达到时确保缓冲区存满 N 个历元，但只用最新两个历元计算差分速度（详见第 9.3 节） | 本项目新增 |

### 5.4 动态初始化默认模式选择（基于 GNSS 解算模式）

> **用户原话**：如果进行动态初始化（当为 spp 模式时，默认使用速度矢量（通过多普勒测速得到的速度）初始化；为 rtk/rtd 模式时，默认使用位置差分初始化；动态初始化需要设置运动阈值）；当动态模式下，GNSS 数据不提供速度值时，统一使用位置差分法。

#### 5.4.1 默认规则

动态初始化（`IN_MOTION` 模式）下，根据 GNSS 解算模式自动选择初始化方法：

| GNSS 解算模式 | 默认动对准方法 | 速度来源 | 原因 |
|--------------|--------------|---------|------|
| **SPP** | 速度矢量初始化（第 8 节 MotionAligned） | 多普勒测速 `sol.vel` | SPP 位置精度差（~10m），位置差分得到的速度不可信；多普勒测速直接基于载波观测，精度独立于位置精度 |
| **RTK Fixed / RTK Float** | 位置差分初始化（第 9 节 InterpolateGnssVel） | 相邻历元位置差分 | RTK 位置精度高（~0.02-0.1m），位置差分速度精度优于多普勒；且 RTK 在低速场景多普勒可能受周跳影响 |
| **RTD** | 位置差分初始化（第 9 节 InterpolateGnssVel） | 相邻历元位置差分 | RTD 位置精度优于 SPP（~1m），位置差分速度可用 |
| **External** | 按 `gnss_velocity_fallback` 配置 | 配置决定 | 外部数据源情况复杂，由用户配置 |

**配置覆盖**：用户可通过 `alignnment_dynamic_method` 显式指定，覆盖默认规则：

| `alignnment_dynamic_method` 值 | 含义 |
|-------------------------------|------|
| `"auto"`（默认） | 按 GNSS 解算模式自动选择（上表） |
| `"velocity_vector"` | 强制使用速度矢量初始化（第 8 节） |
| `"position_diff"` | 强制使用位置差分初始化（第 9 节） |

#### 5.4.2 GNSS 不提供速度时的统一回退策略

> **用户原话**：当动态模式下，GNSS 数据不提供速度值时，统一使用位置差分法。

无论 GNSS 解算模式如何（SPP / RTK / RTD），只要 `GnssSolution.vel` 为 `None` 或无效，**统一使用位置差分法**（第 9 节 InterpolateGnssVel）。位置差分需维护 `gnss_buffer_size`（默认 3）长度的 GNSS 历元滑动窗口，但**只用最新的两个历元**计算差分速度：

```
function resolve_gnss_velocity(gnss, gnss_buffer, config) -> vel_ecef:

  # 1. 检查 GNSS 是否直接提供速度
  if gnss.vel is not None and is_valid(gnss.vel):
    return gnss.vel  # 直接使用 GNSS 速度

  # 2. GNSS 不提供速度，统一使用位置差分法
  #    将当前历元加入缓冲区（维护 gnss_buffer_size 长度的滑动窗口）
  gnss_buffer.append(gnss)
  if len(gnss_buffer) > config.gnss_buffer_size:
    gnss_buffer.pop(0)  # 移除最旧的

  # 3. 缓冲区不足两个历元，无法差分
  if len(gnss_buffer) < 2:
    return None  # 继续等待

  # 4. 只用最新的两个历元计算位置差分速度
  prev_gnss = gnss_buffer[-2]  # 倒数第二个（最新前一个）
  curr_gnss = gnss_buffer[-1]  # 最新一个
  dt = curr_gnss.timestamp - prev_gnss.timestamp
  if dt <= 0:
    return None  # 时间戳异常
  return (curr_gnss.pos - prev_gnss.pos) / dt
```

**注意**：
- 位置差分需要两个相邻 GNSS 历元，第一个 GNSS 历元到达时无法差分，需继续等待第二个
- **GNSS 历元缓冲区**：`gnss_buffer` 维护 `gnss_buffer_size`（默认 3）个历元的滑动窗口，提供历史冗余；但差分计算**只用最新两个历元**，保证速度反映最新运动状态
- SPP 模式下若 GNSS 不提供速度，仍使用位置差分（虽有精度损失，但保证流程可继续）
- 位置差分速度的协方差由位置协方差传播：`vel_cov = (pos_cov1 + pos_cov2) / dt²`

#### 5.4.3 三组独立阈值（static_speed / dynamic_speed / angular_velocity）

> **用户原话**：静态检测的阈值是 0.5m/s，但是动态初始化需要角速度的范数需要小于 (30.0×(π/180.0)) rad/s，差分速度和直接计算的速度范数需要大于 4m/s。

**作用**：判定车辆是否处于运动状态、是否可进入动对准。本项目采用**三组独立阈值**替代单一 `motion_threshold`，分别约束静态检测、动态速度、动态角速度。

**配置项**（`data/config.yaml` 的 `ins` 段）：

| 配置项 | 类型 | 默认值 | 含义 |
|--------|------|--------|------|
| `static_speed_threshold` | double | 0.5 | **静态检测速度阈值** (m/s)：GNSS 速度范数 < 此值判定为静态，进入静对准 |
| `dynamic_speed_threshold` | double | 4.0 | **动态速度阈值** (m/s)：GNSS 速度范数 > 此值才可进入动对准（适用于速度矢量法与位置差分法） |
| `angular_velocity_threshold_deg` | double | 30.0 | **动态角速度阈值** (deg/s)：陀螺角速度范数 < 此值才可进入动对准（≈0.5236 rad/s） |
| `motion_threshold` | double | 0.5 | **已废弃**（保留兼容），由上述三阈值替代 |

**判定逻辑**（实际实现于 `InsInitializer.initialize()`）：

```
function check_init_thresholds(gnss, imu_list, mode) -> bool:

  speed = norm(gnss.vel_ecef)  # GNSS 速度范数

  # 1. 静态模式: 速度必须 < static_speed_threshold
  if mode == STATIC:
    if speed >= config.static_speed_threshold:
      raise ValueError("静态初始化要求速度 < static_speed_threshold")
    return True

  # 2. 动态模式 (VELOCITY_VECTOR / POSITION_DIFF):
  #    2a. 速度必须 > dynamic_speed_threshold
  if speed <= config.dynamic_speed_threshold:
    raise ValueError("动态初始化要求速度 > dynamic_speed_threshold")

  #    2b. 陀螺角速度范数必须 < angular_velocity_threshold
  gyro_norm = compute_gyro_norm(imu_list, gnss.timestamp, window=1.0)
  if gyro_norm >= config.angular_velocity_threshold:
    raise ValueError("动态初始化要求角速度 < angular_velocity_threshold")

  return True
```

**陀螺角速度范数计算**（`_compute_gyro_norm()` 方法）：

```
function compute_gyro_norm(imu_list, t_gnss, window=1.0) -> float:

  # 取 GNSS 时间戳前后 ±1s 窗口内的 IMU 历元
  t_start = t_gnss - window
  t_end = t_gnss + window
  window_imus = [imu for imu in imu_list if t_start <= imu.timestamp <= t_end]

  if not window_imus:
    window_imus = list(imu_list)  # 窗口为空时使用全部 IMU 数据
  if not window_imus:
    return float('inf')  # 无 IMU 数据时不允许动对准

  # 计算窗口内陀螺角速度范数的平均值
  gyro_norms = [norm(imu.gyro) for imu in window_imus]
  return mean(gyro_norms)
```

**阈值选择建议**：

| 场景 | `static_speed_threshold` | `dynamic_speed_threshold` | `angular_velocity_threshold_deg` | 原因 |
|------|--------------------------|---------------------------|----------------------------------|------|
| 默认（车辆） | 0.5 m/s | 4.0 m/s | 30.0 deg/s | 区分静止与低速行驶；角速度约束避免转弯时初始化 |
| 行人/低速车辆 | 0.2 m/s | 1.0 m/s | 20.0 deg/s | 避免低速被误判为静止 |
| 高速车辆 | 1.0 m/s | 5.0 m/s | 30.0 deg/s | 避免停车等红灯时误判为运动 |
| 严格静态启动 | 0.1 m/s | — | — | 仅在确实静止时进入静对准 |

**注意**：
- 三组阈值相互独立，分别约束不同维度
- `static_speed_threshold` 用于静对准（速度必须低于此值）
- `dynamic_speed_threshold` 用于动对准（速度必须高于此值）
- `angular_velocity_threshold_deg` 仅用于动对准（陀螺范数必须低于此值），避免在车辆转弯等高角速度场景下初始化
- 阈值过低会导致 GNSS 速度噪声（SPP 多普勒噪声 ~0.1 m/s）触发误判
- `motion_threshold` 字段保留兼容性，但 `InsInitializer` 不再读取，仅由历史配置文件兼容

#### 5.4.4 默认规则整合到决策树

将本节规则整合到第 5.2 节决策树的步骤 4，更新后的步骤 4 为：

```
步骤 4: 根据 GNSS 速度与三阈值选择对准模式

  ┌──────────────────────────────────────────────────────────────┐
  │ 4.1 解算 GNSS 速度                                          │
  │   - 若 gnss.vel 有效 → 直接使用                             │
  │   - 若 gnss.vel 无效 → 统一使用位置差分法（第 9 节）        │
  │     维护 gnss_buffer_size（默认 3）历元滑动窗口，           │
  │     只用最新两个历元计算差分；缓冲区不足则继续等待         │
  └──────────────────────────────────────────────────────────────┘
                            ↓
  ┌──────────────────────────────────────────────────────────────┐
  │ 4.2 检查 ‖gnss.vel‖ vs static_speed_threshold               │
  │                                                            │
  │   ┌────────────────────────────────────────┐                │
  │   │ < static_speed_threshold (0.5 m/s)     │                │
  │   │   → AlignedMode = STATIC               │                │
  │   │   → 静态初始化（第 7 节 AcceLeveling） │                │
  │   └────────────────────────────────────────┘                │
  │   ┌────────────────────────────────────────┐                │
  │   │ > dynamic_speed_threshold (4.0 m/s)     │                │
  │   │   → 进一步检查陀螺角速度范数            │                │
  │   │     (compute_gyro_norm, ±1s 窗口均值)  │                │
  │   │                                                            │                │
  │   │     ┌──────────────────────────────────────────────┐    │                │
  │   │     │ gyro_norm < angular_velocity_threshold       │    │                │
  │   │     │   (30 deg/s ≈ 0.5236 rad/s)                  │    │                │
  │   │     │   → AlignedMode = IN_MOTION                  │    │                │
  │   │     │   → 根据配置 alignnment_dynamic_method 选择: │    │                │
  │   │     │     "auto"（默认）:                          │    │                │
  │   │     │       - SPP → 速度矢量初始化（第 8 节）      │    │                │
  │   │     │       - RTK/RTD → 位置差分初始化（第 9 节）  │    │                │
  │   │     │     "velocity_vector" → 强制第 8 节          │    │                │
  │   │     │     "position_diff" → 强制第 9 节            │    │                │
  │   │     │     注意：若 GNSS 不提供速度，                │    │                │
  │   │     │     统一使用位置差分法（第 9 节）            │    │                │
  │   │     └──────────────────────────────────────────────┘    │                │
  │   │     ┌──────────────────────────────────────────────┐    │                │
  │   │     │ gyro_norm ≥ angular_velocity_threshold       │    │                │
  │   │     │   → 拒绝本历元，继续等待 (车辆转弯等)        │    │                │
  │   │     └──────────────────────────────────────────────┘    │                │
  │   └────────────────────────────────────────┘                │                │
  │   ┌────────────────────────────────────────┐                │
  │   │ 介于两阈值之间 (0.5 ≤ speed ≤ 4.0)     │                │
  │   │   → AlignedMode = STATIONARY_AND_MOTION │                │
  │   │   → 持续监测，速度超 dynamic_speed_     │                │
  │   │     threshold 后进入动对准              │                │
  │   │   → 【第 10 节】                        │                │
  │   └────────────────────────────────────────┘                │
  └──────────────────────────────────────────────────────────────┘
```

---

## 6. 配置设定初始化（SETTING）

### 6.1 触发条件

参考 `navinitialized.cc:118-181`，配置设定模式有两种子模式：

1. **`use_set_posvelatt = 1`**：完整 PVA（位置/速度/姿态）均来自配置文件
   - 直接使用配置的 `initial_pos` / `initial_vel` / `initial_att`
   - **不消费 GNSS 数据流**（跳过 GNSS 等待）
   - 时间戳取配置的起始时间（`GetStartAndEndTime` 的 `start_time`）
   - 协方差使用固定小值（`pos_std=[0.1,0.1,0.1]`, `vel_std=[0.01,0.01,0.01]`, `att_std=[0.01,0.01,0.01]`）

2. **`use_set_attitude = 1`**：仅姿态来自配置，位置/速度来自 GNSS
   - 等待第一个 GNSS 历元
   - 位置/速度取自 GNSS 解算结果
   - 姿态取自配置的 `initial_att`
   - 协方差：位置/速度使用 GNSS 解算的协方差，姿态使用配置的 `initial_att_std`

### 6.2 处理流程（伪代码）

```
function align_setting(aligned_block, config) -> InsState:

  if config.use_set_posvelatt:
    # 完整配置模式
    pos_llh = config.initial_pos              # [lat, lon, h] (度/度/米)
    vel_ned = config.initial_vel              # [vN, vE, vD] (m/s)
    att_rpy = config.initial_att * deg2rad    # [roll, pitch, yaw] (rad)
    timestamp = config.start_time             # Unix 秒

    # 协方差使用固定小值
    pos_std = [0.1, 0.1, 0.1]
    vel_std = [0.01, 0.01, 0.01]
    att_std = [0.01, 0.01, 0.01]

  elif config.use_set_attitude:
    # 仅姿态配置模式
    gnss = aligned_block.gnss
    pos_ecef = gnss.pos                       # ECEF (m)
    vel_ecef = gnss.vel                       # ECEF (m/s)
    pos_llh = ecef2llh(pos_ecef)              # 转换为纬经高
    vel_ned = C_e^n @ vel_ecef                # 转换为 NED
    att_rpy = config.initial_att * deg2rad
    timestamp = gnss.timestamp

    # 协方差使用 GNSS 解算的协方差
    pos_std = sqrt(diag(gnss.cov[0:3,0:3]))
    vel_std = sqrt(diag(gnss.cov[3:6,3:6]))
    att_std = config.initial_att_std * deg2rad

  # n 系 PVA → E 系 PVA（参考 AttCaln2e, navinitialized.cc:23-29）
  C_n_e = CalCe2n(pos_llh.lat, pos_llh.lon).T  # n→e 旋转矩阵
  C_b_n = euler2dcm(att_rpy)                    # b→n 旋转矩阵
  C_b_e = C_n_e @ C_b_n                         # b→e 旋转矩阵
  vel_e = C_b_n.T @ vel_ned  或  C_e^n.T @ vel_ned
         # navinitialized.cc:138 用 rotation_ (即 C_b_n) 转换速度，
         # 注意其代码使用 att 表示车体姿态，这里需根据实际定义确认
  pos_e = llh2ecef(pos_llh)

  return InsState(timestamp, pos_e, vel_e, C_b_e, att_rpy, pos_std, vel_std, att_std)
```

### 6.3 适用场景

- **已知初始状态**：车辆静态启动且已知精确位置/姿态（如基站、静态测试）
- **测试与调试**：跳过自动对准，快速进入机械编排阶段
- **IMU 故障恢复**：IMU 数据中断后重新启动，使用上次已知状态作为初值

### 6.4 注意事项

- `use_set_posvelatt = 1` 模式下，`navinitialized.cc:124-125` 实际代码中位置/速度仍取自 `gnss_data->pos_/vel_`，这是历史遗留问题；本项目指导方案建议遵循配置值（更符合"配置设定"语义）
- 配置的位置/速度单位需明确：本项目约定位置为 LLH（纬度/经度/高程），速度为 NED（北/东/下），姿态为 RPY（横滚/俯仰/航向），与 KF-GINS 一致
- 协方差使用固定小值意味着对配置值高度信任，仅在配置值确实精确时使用

---

## 7. 静态初始化

### 7.1 触发条件

当**首次 GNSS 速度 < `velocity_threshold`** 且**未启用配置设定模式**时，进入静态初始化（参考 `navinitialized.cc:189-228`）。

**注意**：gnss_ins_lc_nhc 中静态模式实际是 `STATIONARY_AND_MOTION`，即"静止启动持续监测直到速度超阈值"。纯粹的静态初始化（车辆始终静止）是其子情况。本节描述纯粹的静态对准计算，第 10 节描述带监测的 `STATIONARY_AND_MOTION` 模式。

### 7.2 加速度计水平对准（AcceLeveling）

参考 [tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc](file:///home/mxl/workplace/gipylib/tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc) 的 `AcceLeveling` 函数（行 47-59）：

```cpp
Euler AcceLeveling(std::vector<ImuData::Ptr> &imu_buffer) {
    using constant::constant_g0;
    ImuData::Ptr imu = imu_buffer.front();
    auto start_time = imu->get_time();
    imu = std::accumulate(imu_buffer.begin() + 1, imu_buffer.end(), imu, imu_accumulate());
    imu->acce_ /= (imu->get_time() - start_time);  # 时间平均
    imu->gyro_ /= (imu->get_time() - start_time);

    Euler euler(0, 0, 0);
    euler(0) = asin(imu->acce_(1) / constant_g0) * (imu->acce_[2] > 0 ? 1 : -1);  # roll
    euler(1) = asin(imu->acce_(0) / constant_g0) * (imu->acce_[2] > 0 ? -1 : 1);   # pitch
    return euler;
}
```

**数学原理**：

车辆静止时，加速度计感知的比力 `f^b` 主要由重力引起：`f^b ≈ -g^b`（即重力在 b 系的投影取反）。

设 b 系为 FRD（前-右-下），n 系为 ENU（东-北-天），重力在 n 系下为 `[0, 0, -g]`，则：

```
f^b = C_b^n^T @ f^n = C_b^n^T @ [0, 0, -g]
     = [-g * sin(pitch), g * sin(roll) * cos(pitch), -g * cos(roll) * cos(pitch)]
```

因此（参考 `navinitialized.cc:56-57`，注意其 b 系定义可能略有差异）：

```
roll  = asin(f_b[1] / g)  * sign(f_b[2])    # 右向加速度 → 横滚
pitch = asin(f_b[0] / g)  * -sign(f_b[2])   # 前向加速度 → 俯仰（取反，因 FRD 定义）
yaw   = ?                                   # 无法由加速度计确定
```

**航向角无法由加速度计确定**：
- 高精度 IMU：可通过陀螺仪感知地球自转分量确定方位角
- MEMS IMU：地球自转分量低于噪声水平，**必须借助 GNSS 速度方向或配置值**

### 7.3 处理流程（伪代码）

```
function align_static(aligned_block, imu_buffer, config) -> InsState:

  gnss = aligned_block.gnss
  pos_e = gnss.pos                              # ECEF 位置
  pos_llh = ecef2llh(pos_e)                     # 转换为 LLH
  timestamp = gnss.timestamp

  # 1. 加速度计水平对准（AcceLeveling）
  # 对 imu_buffer 做时间平均（参考 navinitialized.cc:50-54）
  f_mean = average([imu.accel for imu in imu_buffer])
  omega_mean = average([imu.gyro for imu in imu_buffer])
  g = 9.7803267715  # 重力常数 constant_g0

  # 计算 roll/pitch（FRD 定义）
  roll  = asin(f_mean[1] / g) * sign(f_mean[2])
  pitch = asin(f_mean[0] / g) * -sign(f_mean[2])
  yaw = config.initial_att[2] * deg2rad  # 默认取配置航向，或由后续 GNSS 速度方向确定

  att_rpy = [roll, pitch, yaw]

  # 2. 位置/速度来自 GNSS
  vel_e = gnss.vel                              # ECEF 速度（静止时应接近 0）

  # 3. 转换到 E 系（参考 AttCaln2e, navinitialized.cc:23-29）
  C_n_e = CalCe2n(pos_llh.lat, pos_llh.lon).T
  C_b_n = euler2dcm(att_rpy)
  C_b_e = C_n_e @ C_b_n

  # 4. 协方差（参考 navinitialized.cc:271-273，STATIONARY_AND_MOTION 模式默认值）
  pos_std = sqrt(diag(gnss.cov[0:3,0:3]))
  vel_std = sqrt(diag(gnss.cov[3:6,3:6]))
  att_std = [2.0_deg, 2.0_deg, 5.0_deg]  # roll/pitch 较准（加速度计对准），yaw 较松（MEMS 不可信）

  return InsState(timestamp, pos_e, vel_e, C_b_e, att_rpy, pos_std, vel_std, att_std)
```

### 7.4 适用场景

- 车辆静止启动，且已知初始航向（配置或外部传感器）
- 高精度 IMU（战术级以上）可通过陀螺仪感知地球自转确定航向
- MEMS IMU 静态对准后，航向误差较大，需依赖后续 GNSS 速度更新逐步收敛

### 7.5 注意事项

- **IMU 数据时长**：建议至少 10 秒静态数据做平均（参考 `navinitialized.cc:199`：`data_rate * 10` 缓冲区）
- **加速度计零偏影响**：`f_b[1]` 包含零偏，会引入 `asin(bias/g)` 的姿态误差；建议先做零偏标定
- **平台水平度**：若车辆停在斜坡上，加速度计对准的 roll/pitch 包含斜坡角度
- **本项目坐标系差异**：本项目主计算在 E 系，AcceLeveling 在 b 系下做平均后转 E 系；gnss_ins_lc_nhc 在 n 系下计算后通过 `AttCaln2e` 转 E 系

---

## 8. 动态初始化 - 速度矢量初始化

### 8.1 触发条件

当**首次 GNSS 速度 > `velocity_threshold`** 且**未启用配置设定模式**时，进入动态初始化-速度矢量模式（参考 `navinitialized.cc:182-188`）。

### 8.2 MotionAligned 算法

参考 [tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc](file:///home/mxl/workplace/gipylib/tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc) 的 `MotionAligned` 函数（行 31-46）：

```cpp
NavInfo &MotionAligned(const GnssData::Ptr &gnss_data, NavInfo &nav_info) {
    nav_info.pos_ = gnss_data->pos_;
    nav_info.vel_ = gnss_data->vel_;
    Vector3d blh = earth::WGS84XYZ2BLH(nav_info.pos_);
    Vector3d vel_ned = earth::CalCe2n(blh(0), blh(1)) * nav_info.vel_;
    nav_info.att_(0) = 0.0;                                                    # roll = 0
    nav_info.att_(1) = atan2(-vel_ned(2), sqrt(pow(vel_ned(0), 2) + pow(vel_ned(1), 2)));  # pitch
    nav_info.att_(2) = atan2(vel_ned(1), vel_ned(0));                          # yaw
    nav_info.rotation_ = Euler2RotationMatrix(nav_info.att_);
    nav_info.quat_ = RotationMartix2Quaternion(nav_info.rotation_);
    nav_info.time_ = gnss_data->get_time();
    nav_info.pos_std_ = gnss_data->pos_std_;
    nav_info.vel_std_ = gnss_data->vel_std_;
    return nav_info;
}
```

**数学原理**：

车辆运动时，假设车辆在水平面运动且无侧滑，则：
- **航向角（yaw）**：由 GNSS 速度方向确定，`yaw = atan2(v_E, v_N)`（NED 速度的东向/北向分量）
- **俯仰角（pitch）**：由 GNSS 速度的垂向分量与水平分量的比值确定，`pitch = atan2(-v_D, sqrt(v_N² + v_E²))`
- **横滚角（roll）**：假设为 0（车辆水平运动，无侧倾）

```
yaw   = atan2(v_E, v_N)
pitch = atan2(-v_D, sqrt(v_N² + v_E²))
roll  = 0
```

其中 `v_N`, `v_E`, `v_D` 为 GNSS 速度在 NED 坐标系下的分量，由 ECEF 速度转换得到：
```
v_ned = C_e^n @ v_ecef
```

### 8.3 处理流程（伪代码）

```
function align_motion_velocity(aligned_block, config) -> InsState:

  gnss = aligned_block.gnss
  pos_e = gnss.pos                                # ECEF 位置
  vel_e = gnss.vel                                # ECEF 速度
  timestamp = gnss.timestamp

  # 1. 转换为 LLH（用于构造 C_e^n）
  pos_llh = ecef2llh(pos_e)                       # [lat, lon, h]

  # 2. ECEF 速度 → NED 速度
  C_e_n = CalCe2n(pos_llh.lat, pos_llh.lon)       # e→n 旋转矩阵
  vel_ned = C_e_n @ vel_e                         # [v_N, v_E, v_D]

  # 3. 计算姿态（参考 MotionAligned, navinitialized.cc:37-39）
  roll  = 0.0
  pitch = atan2(-vel_ned[2], sqrt(vel_ned[0]^2 + vel_ned[1]^2))
  yaw   = atan2(vel_ned[1], vel_ned[0])           # 注意 atan2 参数顺序：东向, 北向
  att_rpy = [roll, pitch, yaw]

  # 4. 构造旋转矩阵并转 E 系（参考 AttCaln2e, navinitialized.cc:23-29）
  C_b_n = euler2dcm(att_rpy)
  C_n_e = C_e_n.T                                 # n→e
  C_b_e = C_n_e @ C_b_n                           # b→e

  # 5. 协方差（参考 navinitialized.cc:270-271，IN_MOTION 模式默认值）
  pos_std = sqrt(diag(gnss.cov[0:3,0:3]))
  vel_std = sqrt(diag(gnss.cov[3:6,3:6]))
  att_std = [5.0_deg, 5.0_deg, 5.0_deg]            # 三轴均较松（动对准精度有限）

  return InsState(timestamp, pos_e, vel_e, C_b_e, att_rpy, pos_std, vel_std, att_std)
```

### 8.4 适用场景

- 车辆起始即处于运动状态（如行驶中启动系统）
- 车载 GNSS/INS 系统，初始速度已知且大于阈值
- 不需要静态阶段，快速进入组合导航

### 8.5 注意事项

- **速度阈值**：`velocity_threshold` 默认 0.5 m/s，过低会导致航向角噪声大（低速时 GNSS 速度方向不稳定）
- **假设条件**：车辆水平运动、无侧滑、无侧倾。若实际工况不满足，初始姿态会有偏差，需依赖 EKF 收敛
- **GNSS 速度精度**：动对准精度直接依赖 GNSS 速度精度，建议使用 RTK 速度（精度 ~0.05 m/s）
- **车载设备坐标系差异**：`navinitialized.cc:187` 注释提到"面向车载设备，且设备坐标系与车体系差异不显著"，若 IMU 安装角显著，需先做安装角补偿
- **atan2 参数顺序**：注意 `atan2(v_E, v_N)` 与 `atan2(y, x)` 的对应关系，避免航向角符号错误

---

## 9. 动态初始化 - 位移矢量初始化

### 9.1 触发条件

当**GNSS 数据源仅提供位置（无速度）**时（`gnss.format_type_ == GNSSPOS`，参考 `navinitialized.cc:154-163`），需要通过两个相邻 GNSS 历元的位置差分计算速度，再进入第 8 节的 MotionAligned 流程。

**GNSS 历元缓冲区要求**（用户原话）：采用位置差分初始化方法时，需维护一个长度为 `gnss_buffer_size`（默认 3）的 GNSS 历元滑动窗口。当运动阈值达到时，**确保缓冲区已存满 3 个历元**，但**只用最新的两个历元位置解**计算位置差分速度。若缓冲区未存满，延迟初始化继续等待。

### 9.2 InterpolateGnssVel 算法

参考 [tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc](file:///home/mxl/workplace/gipylib/tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc) 的 `InterpolateGnssVel` 函数（行 17-21）：

```cpp
GnssData::Ptr &InterpolateGnssVel(const GnssData::Ptr &first, GnssData::Ptr &second) {
    second->vel_ = (second->pos_ - first->pos_) / (second->get_time() - first->get_time());
    return second;
}
```

**数学原理**：

```
v_ecef = (pos_ecef[t2] - pos_ecef[t1]) / (t2 - t1)
```

即用两个相邻 GNSS 历元的位置差除以时间差，得到 ECEF 速度。

### 9.3 处理流程（伪代码）

> **核心约束**（用户原话）：采用位置差分初始化方法时，确保要存储之前几个历元的 GNSS 位置解，即确保当前运动阈值要达到的同时，设置为要存储 3 个历元的 GNSS 定位解，但是只用最新的两个历元位置解来计算。

```
function align_motion_displacement(aligned_block, config, gnss_buffer) -> InsState:

  gnss = aligned_block.gnss

  # 1. 检查 GNSS 数据是否包含速度
  if gnss.vel is None or gnss.format_type == GNSSPOS:
    # 仅位置模式，需要 GNSS 历元缓冲区做差分

    # 1.1 将当前历元加入缓冲区（维护 gnss_buffer_size 长度的滑动窗口）
    gnss_buffer.append(gnss)
    if len(gnss_buffer) > config.gnss_buffer_size:  # 默认 3
      gnss_buffer.pop(0)  # 移除最旧的

    # 1.2 缓冲区不足，无法差分
    if len(gnss_buffer) < 2:
      return None  # 继续等待

    # 1.3 用最新的两个历元计算位置差分速度（参考 InterpolateGnssVel, navinitialized.cc:17-21）
    #     注意：即使缓冲区有 3 个历元，也只用最新的两个（gnss_buffer[-2] 和 gnss_buffer[-1]）
    prev_gnss = gnss_buffer[-2]  # 倒数第二个（最新前一个）
    curr_gnss = gnss_buffer[-1]  # 最新一个
    dt_gnss = curr_gnss.timestamp - prev_gnss.timestamp
    if dt_gnss <= 0:
      return None  # 时间戳异常

    vel_e = (curr_gnss.pos - prev_gnss.pos) / dt_gnss
    gnss.vel = vel_e  # 填充速度字段
    gnss.cov[3:6, 3:6] = (curr_gnss.cov[0:3, 0:3] + prev_gnss.cov[0:3, 0:3]) / dt_gnss^2
                        # 速度协方差由位置协方差传播

    # 1.4 检查动态速度阈值是否达到 AND 缓冲区是否存满
    if norm(vel_e) > config.dynamic_speed_threshold:
      if len(gnss_buffer) < config.gnss_buffer_size:
        # 速度阈值达到但缓冲区未存满（< 3 个历元），延迟初始化继续等待
        # 确保"速度阈值达到的同时，缓冲区已存满 3 个历元"
        return None
      # 速度阈值达到且缓冲区存满，继续执行后续对准

  # 2. 速度已就绪，进入速度矢量初始化（第 8 节）
  return align_motion_velocity(aligned_block, config)
```

**关键设计**：
- **缓冲区大小 3，计算只用 2**：`gnss_buffer` 维护 `gnss_buffer_size`（默认 3）个历元，提供历史冗余；但位置差分计算**只用最新的两个历元**（`gnss_buffer[-2]` 和 `gnss_buffer[-1]`），保证速度反映最新运动状态
- **双重门槛**：运动阈值达到 AND 缓冲区存满 3 个历元，两者同时满足才进入对准；任一不满足则延迟初始化
- **历史冗余的用途**：第 3 个历元（最旧的）可作为 fallback，当最新两个历元中有一个出现异常（如周跳、粗差）时，可回退使用 `gnss_buffer[-3]` 与 `gnss_buffer[-1]` 做差分

### 9.4 适用场景

- GNSS 解算结果仅输出位置（如 SPP 模式可能不输出速度）
- 外部 GNSS 文件仅包含位置信息（如某些 POS 文件格式）
- 验证 GNSS 速度精度（与差分速度对比）

### 9.5 注意事项

- **GNSS 历元缓冲区**（用户原话）：采用位置差分初始化方法时，需维护 `gnss_buffer_size`（默认 3）长度的 GNSS 历元滑动窗口；运动阈值达到时确保缓冲区已存满 3 个历元，但**只用最新的两个历元位置解**计算差分速度
- **需要两个 GNSS 历元**：第一个 GNSS 历元到达时无法计算速度，需继续等待第二个
- **时间间隔**：GNSS 采样率 1 Hz 时，差分时间间隔 1 秒，速度精度受位置精度限制（SPP ~10m 位置 → ~10 m/s 速度误差，不可用；RTK ~0.02m 位置 → ~0.02 m/s 速度误差，可用）
- **精度损失**：差分速度精度低于直接多普勒测速，建议优先使用 GNSS 直接输出的速度
- **加速度影响**：若车辆加速度较大，差分速度反映的是 `[t1, t2]` 区间内的平均速度，与 `t2` 时刻的瞬时速度有差异
- **本项目 GNSS 数据源**：`InternalGnssSensor` 输出的 `GnssSolution` 通常包含速度（来自 rtklib-py 的 `sol.rr[3:6]`），故此模式主要用于外部 GNSS 文件场景
- **缓冲区清空时机**：reboot 事件触发时需清空 `gnss_buffer`（详见第 13.6.4 节），避免使用 reboot 前的陈旧历元

---

## 10. 静止启动到动对准（STATIONARY_AND_MOTION）

### 10.1 触发条件

当**首次 GNSS 速度 < `velocity_threshold`** 且**未启用配置设定模式**时，进入 `STATIONARY_AND_MOTION` 模式（参考 `navinitialized.cc:189-228`）。

与第 7 节"静态初始化"的区别：
- **静态初始化**（第 7 节）：车辆始终静止，直接用 AcceLeveling + 配置航向完成对准
- **STATIONARY_AND_MOTION**（本节）：车辆先静止，持续监测，**当速度超阈值时切换到动对准**，融合 AcceLeveling 与 MotionAligned

### 10.2 监测逻辑

参考 `navinitialized.cc:191-227`：

```cpp
aligned_mode_ = STATIONARY_AND_MOTION;
int data_rate = config->get<int>("data_rate");
while (true) {
    data = ptr_data_queue_->GetData();
    if (data->get_type() == IMUDATA) {
        imu_data_buffer.emplace_back(std::dynamic_pointer_cast<ImuData>(data));
        if (imu_data_buffer.size() > data_rate * 10) {
            imu_data_buffer.erase(imu_data_buffer.begin());  # 维护 10 秒滑动窗口
        }
    }
    else if (data->get_type() == GNSSDATA) {
        if (gnss_data->format_type_ == GNSSPOS) {
            # 仅有位置，做差分计算速度
            GnssData::Ptr temp_gnss_data = std::dynamic_pointer_cast<GnssData>(data);
            gnss_data = InterpolateGnssVel(gnss_data, temp_gnss_data);
        }
        if (gnss_data->vel_.norm() > velocity_threshold) {
            # 速度超阈值，切换到动对准
            nav_info = MotionAligned(gnss_data, nav_info);
            Euler euler = AcceLeveling(imu_data_buffer);
            euler(2) = nav_info.att_(2);  # yaw 取自动对准，roll/pitch 取自加速度计
            nav_info.quat_ = Euler2Quaternion(euler);
            nav_info.rotation_ = Quaternion2RotationMatrix(nav_info.quat_);
            nav_info.att_ = euler;
            nav_info = AttCaln2e(nav_info);
            return true;
        }
    }
}
```

### 10.3 处理流程（伪代码）

```
function align_stationary_then_motion(initial_block, config) -> InsState:

  # 1. 进入 STATIONARY_AND_MOTION 模式
  imu_buffer = []  # 维护 data_rate * 10 长度的滑动窗口
  gnss_buffer = []  # 维护 gnss_buffer_size（默认 3）长度的 GNSS 历元滑动窗口

  # 2. 持续消费 estimate_queue，监测速度
  while True:
    aligned_block = estimate_queue.get()  # 等待下一个历元

    # 2.1 IMU 数据入缓冲区（参考 navinitialized.cc:196-202）
    for imu in aligned_block.imu_list:
      imu_buffer.append(imu)
      if len(imu_buffer) > config.data_rate * 10:
        imu_buffer.pop(0)  # 维护滑动窗口

    # 2.2 处理 GNSS 数据
    gnss = aligned_block.gnss
    if gnss is None:
      continue

    # 2.3 仅位置模式，差分计算速度（第 9 节，使用 gnss_buffer）
    if gnss.format_type == GNSSPOS:
      # 2.3.1 将当前历元加入缓冲区（维护 gnss_buffer_size 长度的滑动窗口）
      gnss_buffer.append(gnss)
      if len(gnss_buffer) > config.gnss_buffer_size:
        gnss_buffer.pop(0)

      # 2.3.2 缓冲区不足两个历元，无法差分
      if len(gnss_buffer) < 2:
        continue

      # 2.3.3 只用最新的两个历元计算位置差分速度
      prev_gnss = gnss_buffer[-2]
      curr_gnss = gnss_buffer[-1]
      gnss.vel = (curr_gnss.pos - prev_gnss.pos) / (curr_gnss.timestamp - prev_gnss.timestamp)

    # 2.4 检查动态速度阈值 + 陀螺角速度阈值（第 5.4.3 节三阈值系统）
    if norm(gnss.vel) > config.dynamic_speed_threshold:
      # 速度超阈值，进一步检查陀螺角速度范数
      gyro_norm = compute_gyro_norm(imu_buffer, gnss.timestamp, window=1.0)
      if gyro_norm >= config.angular_velocity_threshold:
        # 角速度过大（车辆转弯等），延迟初始化继续等待
        continue

      # 角速度合格，检查缓冲区是否存满（第 9.3 节双重门槛）
      if gnss.format_type == GNSSPOS and len(gnss_buffer) < config.gnss_buffer_size:
        # 缓冲区未存满，延迟初始化继续等待
        continue

      # 速度与角速度均合格且缓冲区存满，切换到动对准

      # 2.4.1 MotionAligned 计算 yaw（第 8 节）
      pos_llh = ecef2llh(gnss.pos)
      C_e_n = CalCe2n(pos_llh.lat, pos_llh.lon)
      vel_ned = C_e_n @ gnss.vel
      yaw = atan2(vel_ned[1], vel_ned[0])  # 东向, 北向

      # 2.4.2 AcceLeveling 计算 roll/pitch（第 7 节）
      f_mean = average([imu.accel for imu in imu_buffer])
      g = 9.7803267715
      roll  = asin(f_mean[1] / g) * sign(f_mean[2])
      pitch = asin(f_mean[0] / g) * -sign(f_mean[2])

      # 2.4.3 融合：yaw 取自动对准，roll/pitch 取自加速度计（参考 navinitialized.cc:213-218）
      att_rpy = [roll, pitch, yaw]

      # 2.4.4 转换到 E 系（参考 AttCaln2e）
      C_b_n = euler2dcm(att_rpy)
      C_n_e = C_e_n.T
      C_b_e = C_n_e @ C_b_n

      # 2.4.5 协方差（参考 navinitialized.cc:272-273，STATIONARY_AND_MOTION 默认值）
      pos_std = sqrt(diag(gnss.cov[0:3,0:3]))
      vel_std = sqrt(diag(gnss.cov[3:6,3:6]))
      att_std = [2.0_deg, 2.0_deg, 5.0_deg]  # roll/pitch 较准（静对准），yaw 较松（动对准）

      return InsState(gnss.timestamp, gnss.pos, gnss.vel, C_b_e, att_rpy, pos_std, vel_std, att_std)
```

### 10.4 适用场景

- 车辆静止启动，等待起步后完成对准（最常见场景）
- 不依赖配置航向值，全部由传感器观测确定
- 融合静对准（加速度计水平对准）与动对准（GNSS 速度航向）的优势

### 10.5 注意事项

- **IMU 缓冲区长度**：`data_rate * 10` 表示 10 秒滑动窗口，过长会引入运动数据污染，过短会降低平均精度
- **滑动窗口策略**：先进先出（FIFO），保证缓冲区始终包含最近的 10 秒数据
- **超阈值时刻的 IMU 数据**：触发动对准时，缓冲区可能已包含部分运动数据，影响 AcceLeveling 精度；建议在速度超阈值前一小段时间（如 1 秒）的数据用于 AcceLeveling
- **融合策略**：`roll/pitch` 取自 AcceLeveling（精度高），`yaw` 取自 MotionAligned（MEMS 静态无法确定航向）
- **超时处理**：若长时间速度未超阈值，可降级为纯静态初始化（第 7 节），使用配置航向

---

## 11. 初始状态装配

### 11.1 状态向量定义

参考 [tools/KF-GINS/src/kf-gins/gi_engine.cpp](file:///home/mxl/workplace/gipylib/tools/KF-GINS/src/kf-gins/gi_engine.cpp) 的 `initialize` 方法（行 63-90）和 [tools/GINav/src/ins/ins_init.m](file:///home/mxl/workplace/gipylib/tools/GINav/src/ins/ins_init.m)：

初始状态包括：
- **位置** `pos_e` (ECEF, m)：来自 GNSS 解算或配置
- **速度** `vel_e` (ECEF, m/s)：来自 GNSS 解算或配置
- **姿态** `C_b_e` (3×3 旋转矩阵) / `q_b_e` (四元数) / `att_rpy` (欧拉角)：来自对准过程或配置
- **IMU 误差**：
  - 陀螺零偏 `gyro_bias` (rad/s)
  - 加计零偏 `accel_bias` (m/s²)
  - 陀螺比例因子 `gyro_scale` (无量纲，ppm 量级)
  - 加计比例因子 `accel_scale` (无量纲，ppm 量级)
- **安装角** `imu_angle` (2 维，pitch/yaw)：来自配置（后续由 NHC 子滤波估计）
- **IMU 杆臂** `imu_leverarm` (3 维，b 系)：来自配置（后续由 NHC 子滤波估计）
- **GNSS 天线杆臂** `leverarm` (3 维，b 系)：来自配置（可选，由主滤波估计）

### 11.2 单位转换（参考 KF-GINS loadConfig）

参考 [tools/KF-GINS/src/kf_gins.cpp](file:///home/mxl/workplace/gipylib/tools/KF-GINS/src/kf_gins.cpp) 的 `loadConfig` 函数（行 225-361）：

| 配置项 | 配置单位 | 标准单位 | 转换公式 |
|--------|---------|---------|---------|
| `initial_pos[0:2]` (lat, lon) | 度 | 弧度 | `rad = deg * D2R` |
| `initial_pos[2]` (h) | 米 | 米 | 不变（KF-GINS 行 246 有 `*= R2D`，是历史遗留，本项目不采用） |
| `initial_att` | 度 | 弧度 | `rad = deg * D2R` |
| `initial_vel` | m/s | m/s | 不变 |
| `initial_gyro_bias` | °/hr | rad/s | `rad/s = deg/hr * D2R / 3600` |
| `initial_acce_bias` | mGal | m/s² | `m/s² = mGal * 1e-5` |
| `initial_gyro_scale` | ppm | 无量纲 | `= ppm * 1e-6` |
| `initial_acce_scale` | ppm | 无量纲 | `= ppm * 1e-6` |
| `initial_imu_angle` | 度 | 弧度 | `rad = deg * D2R` |
| `initial_imu_leverarm` | 米 | 米 | 不变 |
| `leverarm` (GNSS 杆臂) | 米 | 米 | 不变 |

### 11.3 坐标系转换（n 系 → E 系）

参考 [tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc](file:///home/mxl/workplace/gipylib/tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc) 的 `AttCaln2e` 函数（行 23-29）：

```cpp
NavInfo &AttCaln2e(NavInfo &nav_info) {
    Vector3d blh = earth::WGS84XYZ2BLH(nav_info.pos_);
    nav_info.rotation_ = earth::CalCn2e(blh(0), blh(1)) * nav_info.rotation_;
    nav_info.quat_ = RotationMartix2Quaternion(nav_info.rotation_);
    return nav_info;
}
```

**转换逻辑**：

```
输入：n 系下的姿态 C_b^n（由对准过程计算）
输出：E 系下的姿态 C_b^e

步骤：
  1. 由 ECEF 位置计算 LLH：[lat, lon, h] = WGS84XYZ2BLH(pos_e)
  2. 构造 n→e 旋转矩阵：C_n^e = CalCn2e(lat, lon)
  3. 转换姿态：C_b^e = C_n^e @ C_b^n
  4. 转换四元数：q_b^e = dcm2quat(C_b^e)

速度转换（参考 navinitialized.cc:138）：
  vel_e = C_b^n^T @ vel_ned  或  vel_e = C_n^e^T @ vel_ned
  （具体取决于速度定义在哪个系）
```

### 11.4 装配伪代码

```
function assemble_state(gnss, att_rpy, imu_error_config, config) -> InsState:

  # 1. 位置/速度（ECEF）
  pos_e = gnss.pos
  vel_e = gnss.vel
  timestamp = gnss.timestamp

  # 2. 姿态转换 n 系 → E 系（参考 AttCaln2e）
  pos_llh = ecef2llh(pos_e)
  C_b_n = euler2dcm(att_rpy)              # [roll, pitch, yaw] → C_b^n
  C_n_e = CalCe2n(pos_llh.lat, pos_llh.lon).T   # 注意 CalCn2e 返回的是 C_n→e
  C_b_e = C_n_e @ C_b_n
  q_b_e = dcm2quat(C_b_e)

  # 3. IMU 误差（参考 KF-GINS loadConfig, 行 248-264）
  gyro_bias  = config.initial_gyro_bias  * (D2R / 3600.0)    # °/hr → rad/s
  accel_bias = config.initial_acce_bias  * 1e-5              # mGal → m/s²
  gyro_scale = config.initial_gyro_scale * 1e-6              # ppm → 无量纲
  accel_scale = config.initial_acce_scale * 1e-6             # ppm → 无量纲

  # 4. 安装角与杆臂（参考 navinitialized.cc:102-115）
  imu_angle = config.initial_imu_angle * deg2rad             # [pitch, yaw] (rad)
  imu_leverarm = config.initial_imu_leverarm                 # [lx, ly, lz] (m, b 系)
  leverarm = config.leverarm                                 # GNSS 天线杆臂 (m, b 系)

  # 5. 装配 InsState
  state = InsState(
    timestamp    = timestamp,
    pos_e        = pos_e,
    vel_e        = vel_e,
    C_b_e        = C_b_e,
    q_b_e        = q_b_e,
    att_rpy      = att_rpy,
    gyro_bias    = gyro_bias,
    accel_bias   = accel_bias,
    gyro_scale   = gyro_scale,
    accel_scale  = accel_scale,
    imu_angle    = imu_angle,
    imu_leverarm = imu_leverarm,
    leverarm     = leverarm,
  )

  return state
```

### 11.5 注意事项

- **prevapre 与 pvacur 相同**：参考 KF-GINS `initialize` 行 78，初始化时 `pvapre_ = pvacur_`，即上一时刻状态与当前状态相同，第一次机械编排时 `dt` 由下一个 IMU 历元决定
- **IMU 误差初始值**：若 IMU 已做过标定，使用标定值；否则置零，由 EKF 慢慢收敛
- **安装角与杆臂**：初始化时使用配置值，后续由 NHC 子滤波在线估计
- **坐标系一致性**：所有状态量最终表示在 E 系下，对准过程中产生的 n 系中间量（如 `vel_ned`、`C_b_n`）需立即转回 E 系

---

## 12. 初始协方差矩阵设置

### 12.1 双滤波协方差独立装配

参考 [estimator.md 第 3 节](file:///home/mxl/workplace/gipylib/skills/estimator.md#3-状态向量定义) 的双滤波架构，初始协方差分两套独立装配：

- **主滤波 P1**（E 系，15/18 维）：位置/速度/姿态/陀螺零偏/加计零偏/(GNSS 杆臂)
- **NHC 子滤波 P2**（v 系，5 维）：IMU 安装角(2) + IMU 杆臂(3)

### 12.2 主滤波 P1 初始协方差

参考 [tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc](file:///home/mxl/workplace/gipylib/tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc) 的 `SetInitialVariance` 函数（行 239-303）和 [tools/KF-GINS/src/kf-gins/gi_engine.cpp](file:///home/mxl/workplace/gipylib/tools/KF-GINS/src/kf-gins/gi_engine.cpp) 的 `initialize` 函数（行 80-89）：

```
P1 = diag([
  # 位置 (0:3)，参考 navinitialized.cc:249-260
  pos_std[0]^2, pos_std[1]^2, pos_std[2]^2,

  # 速度 (3:6)，参考 navinitialized.cc:249-260
  vel_std[0]^2, vel_std[1]^2, vel_std[2]^2,

  # 姿态 (6:9)，参考 navinitialized.cc:261-276
  att_std[0]^2, att_std[1]^2, att_std[2]^2,

  # 陀螺零偏 (9:12)，参考 navinitialized.cc:277-280
  gyro_bias_std[0]^2, gyro_bias_std[1]^2, gyro_bias_std[2]^2,

  # 加计零偏 (12:15)，参考 navinitialized.cc:281-282
  accel_bias_std[0]^2, accel_bias_std[1]^2, accel_bias_std[2]^2,

  # GNSS 杆臂 (15:18)，可选，参考 navinitialized.cc:287-291
  # 仅 evaluate_gnss_leverarm=true 时启用
  leverarm_std[0]^2, leverarm_std[1]^2, leverarm_std[2]^2,
])
```

**协方差来源**（参考 `navinitialized.cc:249-303`）：

| 状态 | 来源 | 默认值 |
|------|------|--------|
| 位置/速度 std | `use_define_variance_pos_vel=1` → 配置<br>`use_define_variance_pos_vel=0` → GNSS 解算协方差 | GNSS 协方差 |
| 姿态 std | `use_define_variance_att=1` → 配置<br>`use_define_variance_att=0` → 按对准模式默认 | 按模式（见 12.3） |
| 陀螺零偏 std | 配置 `gyro_bias_std` | 由 IMU 规格确定 |
| 加计零偏 std | 配置 `acce_bias_std` | 由 IMU 规格确定 |
| 比例因子 std | `evaluate_imu_scale=1` → 配置 `gyro_scale_std`/`acce_scale_std` | 由 IMU 规格确定 |
| GNSS 杆臂 std | 配置 `leverarm_std` | 0.5 m |

### 12.3 不同对准模式下的姿态协方差默认值

参考 `navinitialized.cc:261-276`：

```
if use_define_variance_att or aligned_mode == SETTING:
  # 使用配置值
  att_std = config.initial_att_std * deg2rad   # 度 → 弧度
else:
  # 按对准模式默认
  if aligned_mode == IN_MOTION:
    att_std = [5.0_deg, 5.0_deg, 5.0_deg]      # 三轴均较松（动对准精度有限）
  elif aligned_mode == STATIONARY_AND_MOTION:
    att_std = [2.0_deg, 2.0_deg, 5.0_deg]      # roll/pitch 较准（加速度计），yaw 较松（MEMS）
  # SETTING 模式已在上面的 if 分支处理
```

**含义**：
- **SETTING**：使用配置值（通常较小，表示对配置值高度信任）
- **IN_MOTION**：动对准精度有限，三轴均给较松的 5°
- **STATIONARY_AND_MOTION**：roll/pitch 由加速度计对准（精度较好，2°），yaw 由 GNSS 速度方向确定（精度较差，5°）

### 12.4 NHC 子滤波 P2 初始协方差

参考 `navinitialized.cc:294-301`：

```
P2 = diag([
  # IMU 安装角 (0:2)，参考 navinitialized.cc:299
  imu_angle_std[0]^2, imu_angle_std[1]^2,       # pitch, yaw (rad²)

  # IMU 杆臂 (2:5)，参考 navinitialized.cc:300
  imu_leverarm_std[0]^2, imu_leverarm_std[1]^2, imu_leverarm_std[2]^2,  # (m²)
])
```

**默认值**：
- IMU 安装角 std：5°（pitch, yaw）
- IMU 杆臂 std：0.5 m（三轴）

### 12.5 装配伪代码

```
function set_initial_variance(state, gnss, aligned_mode, config) -> (P1, P2):

  # ===== 主滤波 P1 =====
  P1 = zeros((P1_DIM, P1_DIM))   # P1_DIM = 15 或 18

  # 位置/速度协方差（参考 navinitialized.cc:249-260）
  if config.use_define_variance_pos_vel:
    P1[0:3, 0:3] = diag(config.initial_pos_std ** 2)
    P1[3:6, 3:6] = diag(config.initial_vel_std ** 2)
  else:
    P1[0:3, 0:3] = gnss.cov[0:3, 0:3]
    P1[3:6, 3:6] = gnss.cov[3:6, 3:6]

  # 姿态协方差（参考 navinitialized.cc:261-276）
  if config.use_define_variance_att or aligned_mode == SETTING:
    att_std_rad = config.initial_att_std * deg2rad
    P1[6:9, 6:9] = diag(att_std_rad ** 2)
  else:
    if aligned_mode == IN_MOTION:
      att_std_rad = [5.0 * deg2rad, 5.0 * deg2rad, 5.0 * deg2rad]
    elif aligned_mode == STATIONARY_AND_MOTION:
      att_std_rad = [2.0 * deg2rad, 2.0 * deg2rad, 5.0 * deg2rad]
    P1[6:9, 6:9] = diag(att_std_rad ** 2)

  # 陀螺零偏协方差（参考 navinitialized.cc:277-280）
  gyro_bias_std = config.gyro_bias_std * (D2R / 3600.0)   # °/hr → rad/s
  P1[9:12, 9:12] = diag(gyro_bias_std ** 2)

  # 加计零偏协方差（参考 navinitialized.cc:281-282）
  accel_bias_std = config.acce_bias_std * 1e-5            # mGal → m/s²
  P1[12:15, 12:15] = diag(accel_bias_std ** 2)

  # GNSS 杆臂协方差（可选，参考 navinitialized.cc:287-291）
  if config.evaluate_gnss_leverarm and P1_DIM == 18:
    leverarm_std = config.leverarm_std
    P1[15:18, 15:18] = diag(leverarm_std ** 2)

  # ===== NHC 子滤波 P2 =====
  P2 = zeros((5, 5))
  if config.evaluate_imu_angle:
    imu_angle_std = config.imu_angle_std * deg2rad        # 度 → 弧度
    P2[0:2, 0:2] = diag(imu_angle_std ** 2)
    imu_leverarm_std = config.imu_leverarm_std
    P2[2:5, 2:5] = diag(imu_leverarm_std ** 2)

  return P1, P2
```

### 12.6 注意事项

- **协方差为标准差平方**：装配时记得对标准差平方（参考 KF-GINS `initialize` 行 83-89，使用 `cwiseProduct` 即元素乘）
- **单位一致性**：姿态协方差的单位是 rad²，位置协方差是 m²，速度协方差是 (m/s)²，零偏协方差需转换到标准单位
- **对称性保证**：装配后 `P1 = 0.5 * (P1 + P1^T)`，`P2 = 0.5 * (P2 + P2^T)`
- **正定性保证**：所有对角元素必须为正，最小特征值 ≥ 1e-12
- **KF-GINS 的协方差装配**：参考 `gi_engine.cpp:80-89`，KF-GINS 直接使用配置的 `initstate_std` 平方作为对角线，不区分对准模式；本项目参考 gnss_ins_lc_nhc 的更细粒度策略

---

## 13. 初始化完成与机械编排衔接

### 13.1 完成条件

初始化完成的标志：

1. **状态装配完成**：`InsState` 包含有效的位置/速度/姿态/IMU 误差
2. **协方差装配完成**：`P1` 和 `P2` 已初始化
3. **时间戳已设置**：`state.timestamp` 为 GNSS 历元时刻（Unix 秒）
4. **`prev_state` 已赋值**：参考 KF-GINS `initialize` 行 78，`pvapre_ = pvacur_`

### 13.2 进入机械编排的前置条件

参考 [tools/KF-GINS/src/kf_gins.cpp](file:///home/mxl/workplace/gipylib/tools/KF-GINS/src/kf_gins.cpp) 的 `main` 函数（行 132-150）：

```cpp
// 数据对齐
IMU imu_cur;
do {
    imu_cur = imufile.next();
} while (imu_cur.time < starttime);

GNSS gnss;
do {
    gnss = gnssfile.next();
} while (gnss.time <= starttime);

// 添加IMU数据到GIEngine中，补偿IMU误差
giengine.addImuData(imu_cur, true);

// 添加GNSS数据到GIEngine
giengine.addGnssData(gnss);
```

**KF-GINS 的初始化流程**：
1. 读取 IMU 数据直到 `starttime`
2. 读取 GNSS 数据直到 `starttime` 之后
3. 添加第一个 IMU 数据到 GIEngine（`addImuData(imu_cur, true)`，`true` 表示补偿 IMU 误差）
4. 添加第一个 GNSS 数据到 GIEngine（`addGnssData(gnss)`）
5. 进入主循环，处理后续 IMU 数据（`giengine.newImuProcess()`）

**本项目的衔接流程**：

```
InsInitializer.initialize(aligned_block, config) -> InsState:
  ...（第 3-12 节流程）
  ↓
  返回 InsState + (P1, P2)
  ↓

LcIntegration 进入机械编排主循环（estimator.md 第 8 节）:

  # 1. 设置初始状态
  ins_state = InsInitializer.initialize(aligned_block, config)
  inskf.set_state(ins_state)         # 设置 P1 状态
  inskf.set_covariance(P1, P2)       # 设置 P1/P2 协方差

  # 2. 设置 imupre / imucur（参考 KF-GINS addImuData(imu_cur, true)）
  # imupre = None（第一次机械编排时由 imucur 单独处理，或设置 imupre = imucur）
  imupre = aligned_block.imu_list[-1]   # 取对齐到 t_gnss 的 IMU 历元作为 imupre
  # imucur 将由下一个 AlignedBlock 提供

  # 3. 进入主循环
  while control.is_running():
    next_block = estimate_queue.get()
    for imu in next_block.imu_list:
      imucur = imu
      process_epoch(imupre, imucur, next_block.gnss)
      imupre = imucur
```

### 13.3 第一次机械编排的特殊处理

参考 KF-GINS `addImuData` 的 `compensate=true` 参数（行 53-55）：

```cpp
void addImuData(const IMU &imu, bool compensate = false) {
    imupre_ = imucur_;
    imucur_ = imu;
    if (compensate) {
        imuCompensate(imucur_);   # 第一次添加 IMU 时补偿误差
    }
}
```

**第一次机械编排的特殊处理**：
- 初始化完成后，第一个 IMU 历元（即对齐到 `t_gnss` 的 IMU）作为 `imupre`
- 第一个 IMU 历元需要**补偿 IMU 误差**（陀螺零偏、加计零偏、比例因子），参考 `imuCompensate`
- 后续 IMU 历元作为 `imucur`，与 `imupre` 配对进行机械编排
- 第一个机械编排的 `dt` 由 `imucur.timestamp - imupre.timestamp` 计算

### 13.4 与 LcIntegration 的衔接接口

参考 [estimator.md 第 8.1 节](file:///home/mxl/workplace/gipylib/skills/estimator.md#81-完整处理流程)：

```
LcIntegration 主循环:
  │
  ├── 0. 初始化阶段（首次调用 process_epoch 时）
  │   ├── 检测 UNINITIALIZED 状态
  │   ├── 调用 InsInitializer.initialize(aligned_block, config)
  │   ├── 装配 InsState + P1 + P2
  │   ├── 设置 imupre = aligned_block.imu_list[-1]（对齐到 t_gnss 的 IMU）
  │   ├── 补偿 IMU 误差（imuCompensate）
  │   ├── 状态切换为 INITIALIZED
  │   └── 跳过本历元的机械编排，等待下一个 IMU 历元
  │
  ├── 1. 正常处理阶段（INITIALIZED 状态）
  │   ├── 时间对齐（增量切分，第 4 节）
  │   ├── IMU 机械编排（InsCore.update）
  │   ├── EKF 时间更新（time_update P1 + P2）
  │   ├── GNSS 量测更新（meas_update P1）
  │   ├── NHC/ZUPT 量测更新（meas_update P1 + P2）
  │   ├── 双滤波独立反馈（feedback P1 + P2）
  │   └── 输出 Solution
  │
  └── 2. 异常处理
      ├── 初始化失败 → 降级为 GNSS 纯解算前端
      ├── IMU 数据中断 → 标记 INS 不可用，等待恢复后重新初始化
      └── 协方差矩阵异常 → 对称性/正定性修复
```

### 13.5 注意事项

- **初始化可重复触发**：系统启动后首次匹配到 GNSS 时执行初始化；长时间 GNSS 中断后识别 reboot，需要重新进入初始化流程（详见第 13.6 节）
- **imupre 与 imucur 配对**：第一次机械编排需要 `imupre` 与 `imucur` 都有效；初始化完成时设置 `imupre`，下一个 IMU 历元作为 `imucur`
- **GNSS 速度方向作为航向**：动对准模式下，`yaw` 来自 GNSS 速度方向，因此初始化时刻车辆必须有运动（速度超阈值）
- **状态切换**：`UNINITIALIZED → INITIALIZED` 不是单向的；GNSS 中断超阈值或 IMU 数据中断时回退到 `UNINITIALIZED`，等待重新初始化（参考 KF-GINS `addImuData(imu_cur, true)` 的 `true` 参数）
- **协方差反馈**：初始化后第一次 EKF 量测更新会显著降低位置/速度协方差（来自 GNSS），姿态协方差需要较长时间收敛

### 13.6 Reboot 识别与重新初始化

> **用户原话**：注意初始化模式需要识别 reboot，当第一次初始化后，长时间没有 GNSS 观测还要继续进行初始化，与数据对齐统一调度识别 reboot。

#### 13.6.1 设计目标

系统首次初始化成功后进入机械编排主循环。若运行过程中出现以下情况，需要识别为 "reboot"（重启）事件，**回退到 `UNINITIALIZED` 状态，重新进入初始化流程**：

1. **长时间无 GNSS 观测**：GNSS 中断时长超过 `gnss_outage_threshold`（如 30 秒），INS 长时间纯惯导推算导致位置/速度发散，可信度丧失
2. **IMU 数据中断**：IMU 数据流中断，机械编排无法继续
3. **协方差发散**：主滤波 `P1` 对角元素超过 `cov_divergence_threshold`，状态估计已不可信
4. **GNSS 解算模式降级**：从 RTK Fixed 长时间降级到 SPP（可选，根据应用场景配置）

**与数据对齐统一调度识别 reboot**：reboot 识别由 `Aligner`（数据对齐模块）与 `InsInitializer`（初始化模块）**协同完成**：

- `Aligner` 负责检测数据流层面的 reboot 信号（GNSS 中断、IMU 中断、时间戳跳变）
- `InsInitializer` 负责检测状态层面的 reboot 信号（协方差发散、解算模式降级）
- 两者通过共享 `RebootEvent` 事件统一调度

#### 13.6.2 Reboot 触发条件

| 触发条件 | 阈值配置 | 默认值 | 检测方 | 处理 |
|---------|---------|--------|-------|------|
| GNSS 中断超时 | `gnss_outage_threshold` | 30 s | `Aligner` | 标记 GNSS reboot |
| IMU 中断超时 | `imu_outage_threshold` | 1 s | `Aligner` | 标记 IMU reboot |
| 时间戳跳变 | `timestamp_jump_threshold` | 10 s | `Aligner` | 标记时间 reboot |
| 协方差发散 | `cov_divergence_threshold` | 100 m² | `InsInitializer` / `LcIntegration` | 标记状态 reboot |
| GNSS 模式降级 | `gnss_mode_degrade_threshold` | 60 s | `LcIntegration` | 标记模式 reboot（可选） |

#### 13.6.3 与 Aligner 的协同机制

参考 [imu.md 数据流时序](file:///home/mxl/workplace/gipylib/skills/imu.md) 和 [logANDoutput.md Logger 设计](file:///home/mxl/workplace/gipylib/skills/logANDoutput.md)，`Aligner` 是数据对齐的核心调度器，负责维护 IMU/GNSS 时间对齐。reboot 识别应与 `Aligner` 的调度统一：

```
┌─────────────────────────────────────────────────────────────────┐
│ Aligner (数据对齐调度器)                                         │
│                                                                 │
│  harvest() 循环:                                                │
│    1. 从 imu_queue 收割 IMU 数据                                │
│    2. 从 gnss_queue 收割 GNSS 数据                              │
│    3. 检测 reboot 信号:                                         │
│       ├── GNSS 中断超时 → 触发 RebootEvent(GNSS_OUTAGE)         │
│       ├── IMU 中断超时 → 触发 RebootEvent(IMU_OUTAGE)           │
│       └── 时间戳跳变 → 触发 RebootEvent(TIMESTAMP_JUMP)         │
│    4. 输出 AlignedBlock 或 RebootEvent 到 estimate_queue        │
└─────────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────────┐
│ LcIntegration (主循环消费者)                                     │
│                                                                 │
│  process_epoch(item):                                           │
│    if item is RebootEvent:                                      │
│      handle_reboot(item)  # 第 13.6.4 节                        │
│    elif item is AlignedBlock:                                   │
│      if state == UNINITIALIZED:                                 │
│        InsInitializer.initialize(item, config)  # 重新初始化    │
│      else:                                                      │
│        normal_mechanization(item)                               │
└─────────────────────────────────────────────────────────────────┘
```

#### 13.6.4 Reboot 处理流程

```
function handle_reboot(reboot_event):

  ┌──────────────────────────────────────────────────────────────┐
  │ 1. 记录 reboot 事件                                          │
  │   - 日志记录 reboot 类型、时间戳、上下文                     │
  │   - 保存 reboot 前的 InsState 快照（用于事后分析）           │
  └──────────────────────────────────────────────────────────────┘
                            ↓
  ┌──────────────────────────────────────────────────────────────┐
  │ 2. 状态回退                                                  │
  │   - inskf.state = UNINITIALIZED                              │
  │   - 清空 imupre / imucur                                     │
  │   - 清空 InsInitializer 内部缓冲区（imu_buffer, gnss_buffer）│
  │   - 重置 P1 / P2 到默认值（或保留为大型协方差，表示不信任）  │
  └──────────────────────────────────────────────────────────────┘
                            ↓
  ┌──────────────────────────────────────────────────────────────┐
  │ 3. 等待 GNSS 恢复                                            │
  │   - 标记 INS 输出不可用（输出 GNSS 纯解算结果作为前端）      │
  │   - 继续消费 estimate_queue，等待下一个有效 AlignedBlock     │
  │   - GNSS 恢复后重新触发初始化流程（第 3-12 节）              │
  └──────────────────────────────────────────────────────────────┘
                            ↓
  ┌──────────────────────────────────────────────────────────────┐
  │ 4. 重新初始化                                                │
  │   - 接收到有效 AlignedBlock 后，调用 InsInitializer.initialize│
  │   - 重新执行 IMU 插值、模式选择、对准、状态装配              │
  │   - 可选：使用 reboot 前的 InsState 作为先验（若协方差可信）│
  │   - 状态切换 UNINITIALIZED → INITIALIZED                     │
  │   - 恢复 INS 输出                                            │
  └──────────────────────────────────────────────────────────────┘
```

#### 13.6.5 配置项

| 配置项 | 类型 | 默认值 | 含义 |
|--------|------|--------|------|
| `gnss_outage_threshold` | double | 30.0 | GNSS 中断超时阈值（秒），超过则触发 reboot |
| `imu_outage_threshold` | double | 1.0 | IMU 中断超时阈值（秒），超过则触发 reboot |
| `timestamp_jump_threshold` | double | 10.0 | 时间戳跳变阈值（秒），超过则触发 reboot |
| `cov_divergence_threshold` | double | 100.0 | 协方差发散阈值（m²），主滤波 P1 对角元素最大值超过则触发 reboot |
| `enable_gnss_mode_degrade_reboot` | bool | false | 是否启用 GNSS 模式降级 reboot（如 RTK→SPP） |
| `gnss_mode_degrade_threshold` | double | 60.0 | GNSS 模式降级持续时间阈值（秒） |
| `reboot_use_prior_state` | bool | false | reboot 后重新初始化时是否使用前一次状态作为先验 |

#### 13.6.6 注意事项

- **GNSS 中断阈值选择**：30 秒默认值适用于车辆场景；航空场景可放宽到 60 秒（GNSS 短暂遮挡常见）；行人场景应收紧到 10 秒
- **IMU 中断阈值**：1 秒默认值是因为 IMU 通常 100 Hz，中断 1 秒意味着 100 个历元缺失，机械编排无法外推
- **协方差发散阈值**：100 m² 对应位置标准差 10 m，已超出 GNSS SPP 精度，无意义继续推算
- **reboot 后的先验使用**：默认不使用前一次状态作为先验（`reboot_use_prior_state = false`），完全重新初始化；若协方差仍在可信范围内，可启用先验模式加速收敛
- **与 Aligner 的接口**：`Aligner` 输出的 `RebootEvent` 应包含足够信息（事件类型、时间戳、中断时长），便于 `LcIntegration` 决策
- **多次 reboot**：连续多次 reboot 应触发告警，可能是硬件故障或配置问题
- **数据对齐统一调度**：reboot 识别不应在 `InsInitializer` 内部独立完成，而应通过 `Aligner` 统一调度，避免多个模块各自检测导致状态不一致

---

## 14. 决策流程总图

```
═══════════════════════════════════════════════════════════════
  InsInitializer.initialize(aligned_block, config) -> InsState
  （首次初始化 + reboot 重新初始化共用此流程）
═══════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 0: 加载配置项                                       │
  │   - IMU 误差初值（零偏、比例因子）                       │
  │   - 安装角、杆臂                                         │
  │   - static_speed_threshold（静态检测速度阈值，第 5.4.3 节）│
  │   - dynamic_speed_threshold（动态速度阈值，第 5.4.3 节）  │
  │   - angular_velocity_threshold_deg（动态角速度阈值，第 5.4.3 节）│
  │   - gnss_buffer_size（位置差分 GNSS 历元缓冲区，第 9 节）│
  │   - use_set_posvelatt / use_set_attitude                 │
  │   - alignnment_dynamic_method（动对准方法，第 5.4.1 节） │
  │   - reboot 相关阈值（第 13.6.5 节）                      │
  └─────────────────────────────────────────────────────────┘
                            ↓
  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 1: 检查配置设定模式（最高优先级）                   │
  │                                                         │
  │   use_set_posvelatt=1?                                   │
  │     是 →【第 6 节】SETTING (完整 PVA 来自配置)           │
  │     否 ↓                                                │
  └─────────────────────────────────────────────────────────┘
                            ↓
  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 2: 等待第一个 GNSS 历元 + IMU 插值到 GNSS 时间戳   │
  │   【第 4 节】imu_interpolate (参考 KF-GINS)              │
  │                                                         │
  │   ⚠ 强制约束（第 4.5.1 节）:                             │
  │     GNSS 时间戳前后必须各有一个 IMU 历元（包夹区间），  │
  │     除非 GNSS 时间戳与某 IMU 历元刚好一致（< 1ms）       │
  │     不满足 → 延迟初始化，继续等待更多 IMU 数据           │
  │                                                         │
  │   GNSS 速度无效?                                         │
  │     是 → 统一使用位置差分法（第 5.4.2 节、第 9 节）      │
  │           维护 gnss_buffer_size（默认 3）历元缓冲区，    │
  │           只用最新两个历元计算差分；缓冲区不足则继续等   │
  │     否 ↓                                                │
  └─────────────────────────────────────────────────────────┘
                            ↓
  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 3: 检查仅姿态配置模式                              │
  │                                                         │
  │   use_set_attitude=1?                                    │
  │     是 →【第 6 节】SETTING (姿态来自配置，P/V 来自 GNSS) │
  │     否 ↓                                                │
  └─────────────────────────────────────────────────────────┘
                            ↓
  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 4: 根据 GNSS 速度与三阈值选择对准模式              │
  │   【第 5.4 节】                                          │
  │                                                         │
  │   ‖gnss.vel‖ < static_speed_threshold (0.5 m/s)?        │
  │   是 → STATIC（第 7 节）                                │
  │                                                         │
  │   ‖gnss.vel‖ > dynamic_speed_threshold (4.0 m/s)?       │
  │   是 → 进一步检查陀螺角速度范数                         │
  │   gyro_norm < angular_velocity_threshold (30 deg/s)?    │
  │   是 → AlignedMode = IN_MOTION                          │
  │                                                         │
  │   ┌────────────────────────────────────┐                │
  │   │ 是 → AlignedMode = IN_MOTION       │                │
  │   │     动对准方法选择（第 5.4.1 节）:  │                │
  │   │     alignnment_dynamic_method=      │                │
  │   │       "auto":                       │                │
  │   │         SPP → 第 8 节速度矢量       │                │
  │   │         RTK/RTD → 第 9 节位置差分   │                │
  │   │       "velocity_vector" → 第 8 节   │                │
  │   │       "position_diff" → 第 9 节     │                │
  │   │     ⚠ 若 GNSS 不提供速度，          │                │
  │   │       统一使用第 9 节位置差分       │                │
  │   └────────────────────────────────────┘                │
  │                                                         │
  │   ┌────────────────────────────────────┐                │
  │   │ 否 → AlignedMode =                 │                │
  │   │     STATIONARY_AND_MOTION          │                │
  │   │     【第 10 节】静止启动到动对准    │                │
  │   │     持续监测，速度超阈值后：        │                │
  │   │       roll/pitch ← AcceLeveling    │                │
  │   │       yaw ← MotionAligned          │                │
  │   └────────────────────────────────────┘                │
  └─────────────────────────────────────────────────────────┘
                            ↓
  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 5: 装配初始状态                                     │
  │   【第 11 节】                                           │
  │   - 位置/速度（ECEF）                                    │
  │   - 姿态 C_b^e（n 系 → E 系，AttCaln2e）                 │
  │   - IMU 误差（零偏、比例因子，单位转换）                 │
  │   - 安装角、杆臂                                         │
  └─────────────────────────────────────────────────────────┘
                            ↓
  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 6: 装配初始协方差矩阵                               │
  │   【第 12 节】                                           │
  │   - P1 (主滤波, 15/18 维, E 系)                          │
  │     - 位置/速度: GNSS 协方差或配置                       │
  │     - 姿态: 按对准模式默认或配置                         │
  │     - 零偏/比例因子: 配置                                │
  │   - P2 (NHC 子滤波, 5 维, v 系)                          │
  │     - 安装角/杆臂: 配置                                  │
  └─────────────────────────────────────────────────────────┘
                            ↓
  ┌─────────────────────────────────────────────────────────┐
  │ 步骤 7: 返回 InsState，进入机械编排主循环                │
  │   【第 13 节】                                           │
  │   - 设置 imupre = 对齐到 t_gnss 的 IMU                   │
  │   - 补偿 IMU 误差（imuCompensate）                       │
  │   - 状态切换 UNINITIALIZED → INITIALIZED                 │
  │   - LcIntegration 进入 process_epoch 主循环              │
  └─────────────────────────────────────────────────────────┘
                            ↓
                            ↓
  ╔═════════════════════════════════════════════════════════╗
  ║       机械编排主循环（INITIALIZED 状态）                ║
  ║       Aligner 持续检测 reboot 信号（第 13.6 节）        ║
  ╚═════════════════════════════════════════════════════════╝
                            ↓
              ┌─────────────────────────────┐
              │ Aligner 检测到 reboot?      │
              │  - GNSS 中断 > 30s          │
              │  - IMU 中断 > 1s            │
              │  - 时间戳跳变 > 10s         │
              │  - 协方差 > 100 m²          │
              └─────────────────────────────┘
                     │              │
                    否              是
                     │              ↓
                     │   ┌─────────────────────────────────┐
                     │   │ Reboot 处理（第 13.6.4 节）     │
                     │   │  1. 记录 reboot 事件             │
                     │   │  2. 状态回退 UNINITIALIZED       │
                     │   │  3. 清空 imupre/imucur/缓冲区    │
                     │   │  4. 重置 P1/P2                   │
                     │   │  5. 等待 GNSS 恢复               │
                     │   └─────────────────────────────────┘
                     │              ↓
                     │      ┌───────────────┐
                     │      │ GNSS 恢复?     │
                     │      └───────────────┘
                     │              │ 是
                     │              ↓
                     └──────────────┴──→ 回到步骤 0（重新初始化）
                                          ↑
                                          │
                            （首次初始化也从此处进入）

═══════════════════════════════════════════════════════════════
  初始化完成，进入组合导航机械编排 + EKF 主循环
  详见 estimator.md 第 8 节 / imu.md 第 6 节
  Reboot 识别详见第 13.6 节，与 Aligner 统一调度
═══════════════════════════════════════════════════════════════
```

---

## 附录：关键参考代码位置索引

| 参考内容 | 文件 | 行号 |
|---------|------|------|
| AlignedMode 枚举 | `tools/gnss_ins_lc_nhc/include/imu/navinitialized.h` | — |
| StartAligning 主入口 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 67-230 |
| use_set_posvelatt 分支 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 118-141 |
| 等待 GNSS + InterpolateGnssVel | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 143-163 |
| use_set_attitude 分支 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 164-181 |
| IN_MOTION 分支 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 182-188 |
| STATIONARY_AND_MOTION 分支 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 189-228 |
| InterpolateGnssVel 函数 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 17-21 |
| AttCaln2e 函数 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 23-29 |
| MotionAligned 函数 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 31-46 |
| AcceLeveling 函数 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 47-59 |
| SetInitialVariance 函数 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 239-303 |
| SetStateIndex 函数 | `tools/gnss_ins_lc_nhc/src/imu/navinitialized.cc` | 310-341 |
| imuInterpolate 静态方法 | `tools/KF-GINS/src/kf-gins/gi_engine.h` | 90-106 |
| isToUpdate 方法 | `tools/KF-GINS/src/kf-gins/gi_engine.h` | 167 |
| initialize 方法 | `tools/KF-GINS/src/kf-gins/gi_engine.cpp` | 63-90 |
| newImuProcess 方法 | `tools/KF-GINS/src/kf-gins/gi_engine.cpp` | 92-155 |
| imuCompensate 方法 | `tools/KF-GINS/src/kf-gins/gi_engine.cpp` | 157-171 |
| loadConfig 函数 | `tools/KF-GINS/src/kf_gins.cpp` | 225-361 |
| main 函数数据对齐 | `tools/KF-GINS/src/kf_gins.cpp` | 132-150 |
| ins_init.m | `tools/GINav/src/ins/ins_init.m` | 1-56 |
